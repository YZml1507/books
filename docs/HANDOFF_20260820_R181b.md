# 交接书：优化轨 R182b 起——执行 specs/004-warm-voice 的 M0→M4（2026-08-20）

> 本文件由 R181b 写。新窗口把本文件读一遍即可开工，不需要旧对话的任何内容。
> 状态唯一来源是 `specs/004-warm-voice/tasks.md` 的状态列 + `docs/TASK_LEDGER.md`，
> 本文只做指路，不复制它们的事实（L-01 教训：同 state 两份拷贝必打架）。

---

## 你是谁、在哪

- **角色**：优化轨（修复/实现窗口）。审查轨在另一 worktree（books-audit），
  它写的文件（`specs/*/spec.md`、`docs/PHASE.md` 的 CURRENT_PHASE、
  `docs/AUDIT_FINDINGS.md`）对你**只读**。
- **工作目录**：`C:\Users\Lenovo\Desktop\projects\books`，分支 `main`。
- **解释器**：`.venv\Scripts\python.exe`（venv 被 gitignore，两轨共用）。
- **git 锚点**：开工时 `git log --oneline -3` 应看到 `b280ac5`（R181b）或其后。
  若 `git log audit --oneline -1` 比 main 新，先 `git merge audit --no-edit`。
  **不 push**（除非用户明确说）；**绝不动 audit 分支**；绝不改写历史。

## 开场必读（按顺序，读完直接开工，不要停下来问）

1. `docs/PHASE.md` —— 当前阶段 OPTIMIZE、13 道闸门 + 附加闸门 5–9 的命令清单
2. `specs/004-warm-voice/spec.md` —— 审查轨 R123a 收编的规格：
   5 个 User Story + 19 条判据（验收时逐条跑，尤其判据 9 和 15–19）
3. `specs/004-warm-voice/plan.md` —— 施工图：架构、19 判据映射、文案守则、
   与 003 的协调点
4. `specs/004-warm-voice/tasks.md` —— **你的任务清单，按 M0→M4 顺序执行**，
   每完成一小项立刻改状态列并附实测命令
5. `docs/TASK_LEDGER.md` §114–§116 —— 最近三轮来龙去脉（提案→收编→计划）
6. `docs/GOAL.md` §1–§3 —— 授权范围、三个不自主执行的例外（处置=跳过并记录，
   不是停下问）、13 道闸门纪律

## 任务

按 `tasks.md` 执行 **M0（基线冻结）→ M1（voice 层+双模式）→ M2（十二宫+
幸运项）→ M3（海报）→ M4（收尾移交）**。每个里程碑收尾跑全量回归
（13 闸门 + 附加 5–9 + probe_ui_smoke + probe_selftest_regress，只增不减）。

## 硬约束（违反任一条 = 立即回退改动）

1. **M0 未完成前禁止改任何 `src/` `web/` 文件**（判据 9 的前提）。
2. **`src/guji/interpreter.py` 一行不改**——专业模式输出必须逐字节不变，
   `probes/probe_voice_baseline.py`（M0 产物）把关。
3. **不引入任何新外部依赖**。html2canvas 未获用户授权（US5.5），
   海报用原生 Canvas 零依赖实现（plan §1.5）。
4. MBTI 不做（D-147a）；BYOK 不做（D-146a）。
5. warm/十二宫文案只随响应返回、只落 history.db，**不进 corpus.db /
   knowledge.db**；`probe_no_generated_in_corpus` 要扩展覆盖新文案表。
6. 十二宫每条结论必须带真实语料引文（D-148a：《星學大成》十二宫神表，
   注意语料用传统名：秤宫/人馬/磨蝎/隂陽/雙女 ↔ 今名天秤/射手/摩羯/双子/处女）。
7. 提交信息用 R 编号（你从 **R182b** 起），只提交 main。
8. 断言/探针写脚本文件，不写内联 `python -c`（引号坑，GOAL.md 环境注）。

## 给新窗口的行走纪律

- 宪法第一条：任何结论必须附能复现的命令；文档里的数字一律当"待复验"。
- 卡住两次就换任务，卡点写台账，不停等人。
- 每完成一小项：tasks.md 改状态 → 必要时 TASK_LEDGER append → 提交。
- 完成全部或上下文将尽时：tasks.md 状态列就是进度，另写一份
  `docs/HANDOFF_<日期>_<R号>.md` 交接书（照本文格式）供下一个窗口续。

## 背景一段话（为什么做这件事）

用户指示：当前结果内容"普通人看不懂也不想看"，目标用户是小红书 18–30 岁
女性。诊断（台账 §114.2 可复现）：提问排第 5 节只有 1 行、推导链直接暴露、
六爻对提问直接拒答。解法：确定性 warm 文案层（不需要 LLM）+ 双模式一键切回
+ 有出处的幸运项/十二宫 + 零依赖分享海报。审查轨已实测确认十二宫在语料内
（《星學大成》），这是本项目的差异化，必须带引文。
