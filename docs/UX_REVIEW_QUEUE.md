# UX 审查队列（审查轨维护 · 只提问题不动代码）

> 巡检方法：后台 uvicorn（BOOKS_LLM_DISABLE=1，:8183）+ Playwright 390px 手机视口
> 逐页「入口→填表→提交→读结果→全页截图」。截图目录
> `$LOCALAPPDATA/Temp/tour/`（01_home / form_* / result_* / 02b_chat_after /
> 02c_chat_sidebar）。目视评审：vision_analyze 严格评审 + 小红书调性打分。
> 注意：本轮 LLM 总开关关闭，聊天走降级文案路径——该路径本身也是被审对象。

---

## R216a-巡1（首轮巡检 · 2026-08-24）

### U-001 ✅ [已验证 R216b] 🔴 MAJOR｜黄历结果页整体仍是「老黄历工具」，术语裸抛+文言文无翻译+内容干瘪
- 复现：首页 →「今天适合」→ 默认日期 → 查询。
- 截图：`$LOCALAPPDATA/Temp/tour/result_huangli.png`
- 现状：建除「执」、二十八宿「尾」、彭祖百忌文言原文（"乙不栽植，千株不长"）、
  宜忌标签（嫁娶/捕捉/求嗣/立券…）全部裸抛，无一词有通俗解释；配色棕金传统系，
  与首页粉色闺蜜感割裂；缺吉时/冲煞/幸运色等年轻用户关心的内容；整页仅 100 字符，
  是全站最干瘪的结果页。文案库 copy_bank.json 的年轻化改造没有覆盖到这里。
- 期望：宜忌逐条带场景化人话（如「嫁娶=适合领证/订婚官宣」「开市=开业/上新」）；
  彭祖百忌给白话转译或直接收进折叠；建除/宿给一句话解释；补今日吉时与轻决策建议；
  视觉向首页粉色调靠拢。

### U-002 ✅ [已验证 R216b] 🔴 MAJOR｜温柔版八字结果页下半部仍大篇幅裸露专业区，模式名不副实
- 复现：首页 →「今日命盘」→ 提交 → 温柔版往下滚。
- 截图：`$LOCALAPPDATA/Temp/tour/result_bazi.png`
- 现状：「排盘坐标/纳音/十神格局(比肩劫财食神正官七杀偏印…)运算摘要/古籍依据出处」
  在温柔版下原样铺开约 40% 篇幅；「午午自刑」等术语未过 TEN_GOD_WARM 类映射；
  五行分布纯文本数字（木0.3 火2 …）；页面总长超 5 屏。首屏（R215b 改的生辰小卡）
  已达标，但往下滚体验断裂：从「锋利小刀型」人设卡直坠学术论文。
- 期望：温柔版下专业区默认收进单个折叠（如「📜 想看专业依据？」），或至少把
  十神列表整体映射为人话；五行分布做简单可视化；免责声明移到末尾不打断叙事。

### U-003 ✅ [已验证 R216b·叠字标签项待复核] 🔴 MAJOR｜桃花结果页专业数据区裸露 + "weak" 英文直出 + 与免责声明自相矛盾
- 复现：首页 →「桃花运」→ 测桃花。
- 截图：`$LOCALAPPDATA/Temp/tour/result_taohua.png`
- 现状：「仅供娱乐·详细依据见专业模式」下方紧接着裸列 年支/咸池(桃花)/红鸾/
  天喜/落柱/强弱=weak —— 声称依据在专业模式、却把原始坐标直接铺在当前页；
  「强弱」值输出英文 "weak"；8 条键值对纯工具感排版；四柱标签出现「巳巳」式
  重复观感（辛巳·巳 渲染叠字，疑似标签拼接 bug，需优化轨核对）。
- 期望：该数据块默认折叠或删除（温柔版只留人话解读）；weak→「偏弱」；
  核对四柱标签拼接逻辑。

### U-004 ✅ [已修 R216b 续2+续5] 🟠 HIGH｜合婚结果页大运表格信息过载 + 1997 年应期解读逻辑荒谬
- 复现：首页 →「八字合婚」→ 合 婚（默认 1990 男 × 1992 女）。
- 截图：`$LOCALAPPDATA/Temp/tour/result_hehun.png`
- 现状：温柔版下铺 8 行大运冲合表格（1997→2067），全是壬午丁未式干支无翻译；
  解读文案称「1997年前后两人的大运有互动——那段时间适合一起做决定」，而两人
  当时分别是 7 岁和 5 岁，「一起做决定」荒谬，暴露应期文案未按年龄过滤；
  十神互见/偏印食神虽有括号解释但首次出现仍裸抛；「欢喜冤家预定」标题略带负面暗示。
- 期望：大运表默认只显示当前所处运 ±1 或收进折叠；应期建议过滤掉明显不适用的
  年龄段（童年/幼年）；术语首现即人话。

### U-005 ✅ [已验证 R216b续2] 🟠 HIGH｜起名结果 8 个名字同质化严重 + 全部男性化用字，目标用户错配
- 复现：老玩家入口抽屉 →「五行起名」→ 性别选「女」→ 起 名。
- 截图：`$LOCALAPPDATA/Temp/tour/form_qiming.png`、`result_qiming.png`
- 现状：8 个推荐名全为「李柏X」（柏栋/柏梁/柏杰/柏松/柏楠/柏楷/柏榕/柏荣），
  中间字零变化；寓意 7/8 复用「松柏长青」模板；用字栋梁杰楷全部阳刚厚重，
  对 15-25 女性用户（以及选了「女」的父母）完全错配；无换一批/风格筛选；
  日主/大运/五行分布术语裸抛。
- 期望：候选池按性别分支出清雅灵动系用字（妍/芊/茉/萱类）；同批名字中间字去重；
  寓意逐名独立生成；加「换一批」。

### U-006 ✅ [已验证 R216b续2] 🟠 HIGH｜塔罗表单首字段是「Seed」，技术参数裸抛毁沉浸感
- 复现：首页 →「塔罗占卜」。
- 截图：`$LOCALAPPDATA/Temp/tour/form_tarot.png`
- 现状：「Seed（42）」作为第一行第一字段直面普通用户，像忘藏的调试参数；
  结果页也回显「seed 42 · 3 张（固定 seed 必得同样牌面，可复验）」，工程口吻。
  六爻表单同样有「Seed（铜钱起卦）」字段。
- 期望：Seed 收进高级设置或彻底隐藏（后端自动生成）；结果页复验说明改成人话
  或移到专业模式。

### U-007 ✅ [已验证 R216b续2] 🟠 HIGH｜六爻卦象展示为「6--/2-o」文本符号 + 未回答所问之事
- 复现：老玩家入口 →「六爻占卜」→ 所问何事填「最近工作顺利吗」→ 摇 卦。
- 截图：`$LOCALAPPDATA/Temp/tour/result_liuyao.png`
- 现状：六爻用 "6--、5--、4--、3--、2-o、1--" 文本符号表示，倒序排列且无爻位名；
  用户问工作，解读只讲「师卦讲的是组织」「要有章法地推进」，不给吉凶倾向也不给
  时间范围（本周？本月？）；变卦坤「全阴承载」无好坏指向；坐标区出现内部标注
  「G7：无证据不推测」直接面向用户；时间起卦默认年份 1990 而非当前日期。
- 期望：爻象图形化（⚊⚋）+ 上爻~初爻标注；解读先给一句直接结论再展开；
  内部判据编号不得出现在用户界面；时间起卦默认取当天。

### U-008 ✅ [已验证 R216b续3] 🟡 MED｜LLM 关闭时聊天降级文案情感漠视 + 输入框状态不一致
- 复现：任一结果卡点「💬 聊聊这件事」→ 输入「我最近好累，想问问事业」→ 发送。
- 截图：`$LOCALAPPDATA/Temp/tour/02c_chat_sidebar.png`
- 现状：用户刚倾诉疲惫，收到「（聊天功能暂时没开，稍后再来吧）」——纯系统通知
  口吻，零共情；括号+机器腔与品牌人设冲突；且输入框仍保持可输入态，连发连拒。
- 期望：降级文案带情绪承接并引导替代路径（如「你的心事小满收到啦，今天解忧铺
  打烊中～可以先看看上面的牌面指引，明天来找我聊✨」）；DISABLE 态下输入框置灰
  或发送后禁用。

### U-009 ✅ [已验证 R216b续3] 🟡 MED｜首页「凶」红标 + 一星运势缺乏安抚层
- 复现：打开首页（2026-08-24 恰逢凶日）。
- 截图：`$LOCALAPPDATA/Temp/tour/01_home.png`
- 现状：深红圆形「凶」标签压在粉色礼盒上视觉突兀；一星评分语义不明（满分几星？
  代表什么？）；紧邻的解读文字尚有人话，但凶字先入为主制造焦虑，与「好好生活呀」
  的slogan 相悖。
- 期望：凶日用暖橙/柔化处理并配一句安抚话术；星级加图例或改为「今日能量指数」。

### U-010 ✅ [已验证 R216b续3] 🟡 MED｜首页日运区仍有裸术语（庚午/天乙贵人/值官/龙首星/地支自刑）
- 复现：打开首页读日运卡。
- 截图：`$LOCALAPPDATA/Temp/tour/01_home.png`
- 现状：「五行中火土偏旺；有1处地支自刑，宜稳不争；今日日运：庚午」「天乙贵人：
  丑/未」「今日值官：狮子（龙首星）」——底部功能区都写了「大白话解读」，首页
  自己却术语堆砌，体验断层（vision 评审同样点名此为最高优先级问题之一）。
- 期望：每条术语跟一句人话短注（如「地支自刑=今天容易自己跟自己较劲」）。

### U-011 ✅ [已验证 R216b续3] 🟡 MED｜塔罗牌面风格割裂（第三张紫色抽象图案 vs 前两张写实牌面）
- 复现：塔罗 seed 42 抽 3 张。
- 截图：`$LOCALAPPDATA/Temp/tour/result_tarot.png`
- 现状：节制/皇后为写实人物牌，权杖国王却是紫色抽象几何图，视觉连续性断裂，
  疑似某牌图片缺失走了兜底占位图。需优化轨核查图片资源加载链路。
- 期望：三张牌风格统一；排查兜底占位是否被误触发。

### U-012 ✅ [已验证 R216b续3·附观察] 🟢 LOW｜「来了就好。」开场白在所有结果卡重复出现，略显机械
- 复现：连续使用塔罗→六爻→命盘三个功能。
- 截图：result_*.png 各张顶部。
- 现状：同一句问候语三连，第二次见到已无陪伴感，第三次变成模板噪音。
- 期望：从文案池轮换（sha1 盐确定性抽取机制现成，copy_bank.json 加一组问候语即可）。

### U-013 ✅ [已验证 R216b续3] 🟢 LOW｜手机端聊天入口唯一途径是结果卡内按钮，首页无直达
- 复现：首页找聊天入口。
- 截图：`01_home.png`（左下角 recentToggle 仅 52×52 无文字标识，图标含义不明）
- 现状：想闲聊必须先完成一次排盘才能见到「💬 聊聊这件事」；左下角悬浮钮只有
  图形，新用户不知道那是「我的解读/聊天」。
- 期望：recentToggle 加 aria 可见标识或在首页功能区给聊天一个正式入口卡。

---

## 本轮总结（R216a-巡1 · 2026-08-24）

- **覆盖**：首页/日运、chat（含 DISABLE 降级路径）、tarot、bazi（温柔版全文）、
  taohua、hehun、huangli、liuyao、qiming 共 9 条动线，13 张有效截图，
  pageerror/console error 全程 0。
- **总体判断**：首屏与首页功能区（R214b/R215b 成果）调性已达 8.5/10；
  但**结果页纵深全面塌陷**——几乎所有结果卡都是「上半截闺蜜、下半截论文」结构，
  专业数据区在温柔模式下普遍裸露（U-002/003/004）。黄历是最薄弱环节（U-001）。
- **高频根因（供优化轨归组修复）**：
  ① 温柔模式缺一个统一的「专业区默认折叠」策略（U-002/003/004 同构）；
  ② copy_bank.json 年轻化改造未覆盖 huangli/qiming/liuyao 结果文案（U-001/005/007）;
  ③ 面向用户的界面残留工程参数与内部标注（U-006/007 的 Seed、G7）。
- **流程记录**：巡检脚本存于 `tour.py`/`tour2.py`/`chat*.py`（Temp/tour/）；
  read 视图为 R208b 用户裁决移除项，非缺陷，不计条目。
- 待优化轨修复后，本轨将逐条复核并标 [已验证]。

---

## R216b 复核（2026-08-24 · 审查轨）

优化轨提交 app.js/styles.css 修复（未 commit，工作区 diff 已审）。复核方式：
diff 逐行审阅 + Playwright :8184 重走 U-001/002/003 三条动线 +
vision_analyze 目视复评 + 抽跑闸门（selftest 163 PASS / check_warm_voice PASS /
probe_ui_smoke PASS / check_plain_first PASS / baseline_voice sha256 一致）。
复验截图存 `$LOCALAPPDATA/Temp/tour/verify/`（v_huangli/v_bazi/v_taohua.png），
pageerror/console error 全程 0。

- **U-001 黄历 → [已验证]**：宜忌逐条「术语＝人话」（嫁娶＝领证/订婚/官宣）、
  建除一句话解释、值宿龙尾星注释、彭祖百忌收折叠、补确定性「今日一句话」与
  收尾安抚句。vision 调性 9/10。遗留小项（农历/冲煞缺失、「狩猎」映射突兀）
  记为新低优先级观察项，不阻塞关单。
- **U-002 八字温柔版 → [已验证]**：6 项 details 整组收进
  「📜 想看专业依据？（6 项，展开慢慢看）」默认收起，正文区 pageLen 由 ~900+
  降至 432 字符；专业模式分支零改动（baseline_voice 逐字节一致佐证）。
  vision 调性 8.5/10。
- **U-003 桃花 → [已验证·叠字项已答复]**：裸键值块+四柱标签整体收进
  「🔍 想看桃花坐标？」默认收起；weak→偏弱（正则确认英文不再出现）；
  本轮用例下叠字标签未再现。**但代码审阅发现：注释声称的「叠字标签去重」
  在 pill-row 渲染段（app.js:2277-2283）并无对应实现**——本轮未复现可能只是
  测试日期恰好不构成叠字，该子项按未验证处理，请优化轨确认是漏改还是误注。

### 新增观察项（不计入本轮关单，供下轮巡检）

- V-001 🟢 LOW｜黄历缺农历日期与冲煞信息（vision 复评指出，传统核心字段）。
- V-002 🟢 LOW｜HUANGLI_WARM 中「捕捉＝抓机会…」「狩猎＝外出打猎类活动」在
  都市年轻用户语境略突兀，可再打磨。
### V-003 ✅ [已修 R216b 续5] 🟢 LOW｜八字结果页底部法律声明文字（「非生成文本、同输入必同输出」）
  过于技术腔，建议软化或缩小呈现。

---

## ⚠️ 事故通报（R216a-巡2 · 审查轨 · 最高优先级）

**审查轨在复核海报渲染 bug 时误执行了 `git checkout -- web/static/app.js`，
丢弃了优化轨未 commit 的 R216b 全部 app.js 改动（约 +118 行：U-001 黄历
人话映射 / U-002 温柔版专业区折叠 / U-003 桃花坐标折叠与 weak 映射）。**
`styles.css` 的 8 行（warm-pro-fold 折叠样式）幸存未受影响。

已尽力恢复并全部失败：
- `git fsck --lost-found` 全量 dangling blob 扫描——无 JS 文件（均为古籍文本/二进制）；
- git index 未暂存过该版本；
- Playwright chromium profile 缓存为空（16K）；
- 无编辑器本地历史可用。

**当前工作区状态**：app.js = HEAD (cbd9545)，styles.css 带 R216b 8 行。
selftest 复跑 163 PASS（基线本身未破），但 U-001/002/003 的修复内容已不在
代码中，三条 [已验证] 标记随之作废。请优化轨基于本队列文件中 R216b 复核段
的 diff 描述重做修复；审查轨愿配合逐段核对（复核时留有完整 diff 文本于会话记录）。
审查轨对此事故负全责，已将「绝不运行任何改工作区的 git 命令」补入自身铁律。

---

## R216a-巡2 第二轮巡检发现（2026-08-24 · 均基于修复前 HEAD+R216b 状态复测）

> 覆盖：daily 完整解读、分享海报（真实下载产物）、专业模式切换残留、
> 农历输入、起名性别分支、合婚应期文案、塔罗 10 张边界、起名 AI 点评降级。

### U-014 ✅ [已验证 R216b续] 🔴 MAJOR｜分享海报信息卡标签与值同坐标叠印，文字完全不可读
- 复现：八字提交 → 点「📸 分享图」→ 检查下载的 zhiming-poster.png（1080×1440）。
- 截图：`$LOCALAPPDATA/Temp/tour/round2/poster_download2.png`、`poster_crop_card.png`
- 现状：drawPoster 信息卡三行（幸运色/幸运数字/幸运时段）`fillText(r[0],140,y)`
  与 `fillText(r[1],140,y+0)` 同 x 叠印——「幸运数字」和「5·0」重叠成模糊块，
  幸运时段整行不可读。vision 复评确认严重重叠。check_poster 判据只验「非空含
  娱乐标识」，未钉文字可读性，故闸门全绿但产物实际不可用（小红书用户拿到手的
  正是这张图）。另「黄运标」文案疑似截断。
- 期望：值右移至标签之后（如 x=460）；补一张真实下载产物的像素级验收判据。

### U-015 ✅ [已验证 R216b续·覆盖范围确认] 🔴 MAJOR｜专业模式切换后全局残留：新会话所有功能直出内部键名转储
- 复现：任一结果卡切「📐 专业版」（写 localStorage.voiceMode='pro'）→ 回首页
  进其他任意功能 → 结果区直接铺 ten_gods/five_ele 等原始字段转储（实测单页
  34,914 字符、页面高 146,954px）。
- 截图：`$LOCALAPPDATA/Temp/tour/round2/r2b_lunar_pro_leak.png`
- 现状：voiceMode 是全局持久态但没有显眼的「回到温柔版」提醒；普通用户一旦
  点过一次专业版，此后每个功能的体验都变成开发者调试视图（pos：年干 gan：庚
  god：正印…），且不知道怎么切回来。
- 期望：专业模式提供明显的返回路径；或对 renderCalc 转储做键名中文化；
  至少在 pro 态结果卡头部提示「当前为专业视角，点此回温柔版」。

### U-016 ✅ [已验证 R216b续5·附观察] 🟠 HIGH｜daily 完整解读调性塌陷：术语密度远超首页卡片（4/10 vs 首页 8.5）
- 复现：首页 →「🔍 查看完整解读」。
- 截图：`r2_daily_full.png`
- 现状：「决断底下，火多缺木」大字标题晦涩；「压力位/规矩位/同伴力/表达力」
  四词并列无任何解释（TEN_GOD_WARM 只映射不注释，首次出现即裸抛）；
  「这个方向系统没有对应的坐标维度，不硬答」系统腔拒答直接面向用户
  （src/guji/voice.py:289）；地支自刑/天乙贵人/龙首星等再次裸抛。首页卡片
  与完整解读是同一数据的两副面孔，断层比结果页更刺眼。
- 期望：四力量词各带一句短注；拒答话术人话化（如「这个话题盘里没有对应的
  位置，小满不瞎编～」）；核心断语配白话副标。

### U-017 ✅ [已验证 R216b续] 🟠 HIGH｜合婚应期文案逻辑荒谬仍在（U-004 未修，附根因定位）
- 复现：API 直测 1990男×1992女，warm.reply 第 4 条仍为
  「1997年前后两人的大运有互动（合）——那段时间适合一起做决定」（当时 7 岁/5 岁）。
- 根因：src/guji/voice.py:715 无条件取 dayun_hits[0]（最早的大运=童年期），
  且 dayun_hits 自带 start_age_a 字段却未被用于过滤。
- 期望：按 start_age_a 过滤（双方均 ≥16 岁的大运才进文案）；或改措辞为
  「从 XXXX 年起你们进入互动期」不给行为建议。

### U-018 ✅ [已验证 R216b续2] 🟡 MED｜起名寓意模板重复：「松柏长青 · 松柏长青」双拼
- 复现：默认参数起名（李/1990/5/15/男）。
- 截图：`r2_qiming_review.png`
- 现状：李柏松的寓意渲染为「松柏长青 · 松柏长青」——中间字与尾字寓意坐标
  相同时不做去重，直接拼接。且 U-005 主条目（同质化）仍未修：候选池顺序
  使 8 个名字全带「柏」。
- 期望：两字寓意相同去重为一处；U-005 的池子多样性问题一并处理。

### U-019 ✅ [已验证 R216b续5] 🟡 MED｜起名 AI 点评 DISABLE 降级文案过于生硬
- 复现：BOOKS_LLM_DISABLE=1 下起名 → 点「✨ 让 AI 用古籍典故点评这些名字」。
- 现状：按钮点击后原地插入一行「AI 点评暂未开启」，无情绪、无替代引导，
  且按钮不禁用可反复点。
- 期望：降级话术带人设（如「点评的小书童今天休息～名字的寓意卡片里都有说明，
  先看着，明天来听故事版」）；DISABLE 态下按钮置灰加 tooltip。

### U-020 ✅ [已验证 R216b续5] 🟢 LOW｜塔罗一次抽 10 张时解读篇幅失控（1330 字符纯文本流）
- 复现：张数填 10 → 抽 牌。
- 截图：`r2_tarot10.png`
- 现状：10 张牌逐张排列+逐张解读，390px 下页面长 5753px，无分组/折叠；
  「这几句话想对你说」段落对 10 张牌的串联解读变得冗长难读。
- 期望：≥6 张时解读改为按过去/现在/未来…分段折叠或摘要化。

### U-021 ✅ [已验证 R216b续5] 🟢 LOW｜农历输入排盘正常但结果头未标注「按农历换算」
- 复现：更多设置 → 农历 → 提交。
- 截图：`r2_bazi_lunar.png`（注：该截图同时是 U-015 的证据）
- 现状：换算正确（庚午年壬午月癸卯日戊午时），但结果卡没有任何地方告诉用户
  「你输入的是农历、已换算为公历 X」，遇到闰月等特殊情况用户无从核对。
- 期望：农历输入时在生辰小卡里附一行换算说明。

---

## 本轮总结（R216a-巡2 · 2026-08-24）

- **覆盖**：daily 完整解读、分享海报（真实下载验证）、专业模式持久化残留、
  农历输入、起名（UI+API 双路、性别分支、AI 点评降级）、合婚应期（API 直测）、
  塔罗 10 张边界，共 8 条动线；新增 8 条问题（U-014~U-021），其中 MAJOR 2 条。
- **最重要发现**：①分享海报叠印 bug——闸门 check_poster 只验非空不验可读性，
  用户实际拿到的分享图是坏的（U-014）；②专业模式 localStorage 残留会把整个
  产品变成调试视图且无法自行发现退路（U-015）。
- **正面确认**：温柔版回应提问判据实测达标（「最近感情怎么样」首段即回应+
  共情开场，vision 好评）；起名选「女」时 API/UI 均能给出萱芷薇兰类女名
  （R187b 性别软偏好在木行生效），U-005 的「男性化错配」部分不成立，
  但同质化（全带柏/全带萱）与寓意重复仍在。
- **事故**：见上方事故通报——审查轨误回滚了 R216b 的 app.js 修复，需优化轨
  重做；本轮所有发现均基于回滚后状态复测，不受影响。

---

## R216b 重做记录（2026-08-24 · 优化轨，回应事故通报）

审查轨误回滚后，优化轨已按本文件 R216b 复核段的 diff 描述**全量重做**三处修复
（web/static/app.js；styles.css 的 warm-pro-fold 样式未受事故影响、原样保留）：

1. **U-001 黄历**：HUANGLI_WARM/JIANCHU_NOTE/XIUXIU_NOTE 三张写死映射 +
   彭祖百忌收折叠 + 今日一句话/收尾安抚句。V-002 一并处理：
   「捕捉＝把悬着的小事收个尾」「狩猎＝户外撒欢放放电」（原「抓机会」
   「外出打猎类活动」在都市语境突兀，本轮已换）。
2. **U-002 八字温柔版**：renderWarm 内 warm.details 整组收进
   「📜 想看专业依据？（N 项）」折叠；pro 分支零改动。
3. **U-003 桃花**：坐标块收进「🔍 想看桃花坐标？」折叠；
   STRENGTH_CN 映射 weak→偏弱/mid→平稳/strong→偏旺。

**叠字标签待复核项的答复：是误注，非漏改。** R216b 首版注释错误声称做了
「叠字去重」，实际 pill-row 无该逻辑。经 API 直测 + vision 放大复核确认：
1990-05-15 男的月柱与时柱本就都是辛巳，「巳巳」是真实八字事实而非拼接 bug，
去重反而会篡改坐标。首版注释已在重做时删除并改写为核查结论说明。

**实测（Playwright :8185，390px，pageerror 0）**：
huangli 字符数 100→230、裸文言不再直出（has_pengzu_raw=false）、折叠=1；
bazi 温柔版高度 2879→1579px、「排盘坐标/运算摘要」等 pro 标记从可见文本消失；
taohua 折叠 present=true、展开后无英文 strength（weak→偏弱 上屏）。

**闸门（$LOCALAPPDATA/Temp/gates_r216b.log，BOOKS_LLM_DISABLE=1 串行）**：
selftest 163 PASS EXIT=0；probe_ui_smoke PASS 41 用例 EXIT=0；
baseline_voice sha256 一致 EXIT=0；check_warm_voice 判据 1-8 PASS EXIT=0
（--self-check 阳性对照 PASS）；check_plain_first 5×8 PASS EXIT=0。

---

## R216b 续修复记录（2026-08-24 · 优化轨 · U-014/U-015/U-017）

**U-014 海报叠印**（app.js _paintPoster）：
1. 值 x 140→460（原 fillText(r[1],140,y+0) 与标签同点叠印）；
2. 复验发现出处行 y=cardY+330 与第三信息行 y≈cardY+328 叠印——
   卡高 430→560、出处下移至 cardY+480 起（行距 44px）、单行 >26 字截断；
3. 幸运时段三时段一行放不下→拆两行（2+1）；lucky_hours 全量 join 溢出→
   只取前三个并半角压缩。
**像素级验收**（Playwright 真实下载产物 + vision 严格评审，
$LOCALAPPDATA/Temp/tour/r216b2_poster_v5.png）：三行信息完整无叠印、
时段两行排版完整、出处与信息行分离；v1→v5 共四轮目视迭代
（v3 曾因白卡重绘顺序盖掉文字——重绘块已删，v4 验证恢复）。

**U-015 专业模式残留**（renderVoice pro 分支头部 additive）：常显提示条
「📐 当前是专业视角…🌸 回到温柔版」，复用既有 [data-voice] 全局委托
（点击即 setVoiceMode('warm')+rerenderVoice）。pro 渲染内容零改动。
实测：localStorage voiceMode='pro' 下提交 bazi → 提示条 present、
点击返回钮 → voiceMode='warm' 且 L0 人话上屏。

**U-017 合婚应期童年荒谬**（src/guji/voice.py warm_hehun）：按审查轨定位的
根因修——dayun_hits[0] 无条件取最早运。改为只取 start_age_a≥16 的首个
成年运给「适合一起做决定」建议；无合格运时降级为中性描述
「从 XXXX 年起你们进入大运互动期」。dayun_hits 数据零改动
（selftest hehun.dayun 的 8 运/1997 口径不变——那是坐标层，冻结不动；
改的是 warm 文案层的选择逻辑）。API 实测 1990男×1992女：reply 从
「1997年前后…」（7岁/5岁）变为不再含 1997（该对大运仅 2027 一命中，
37.3 岁成年合格）。

**闸门**（gates_r216b2.log，DISABLE=1 串行全 EXIT=0）：selftest 163 ·
ui_smoke · baseline_voice sha256 一致 · warm_voice · plain_first ·
check_poster · guji.voice 自测。回归：huangli/bazi/taohua 三页 pageerror 0、
前批修复数字不回退（230/415/415 字符）。

---

## R216b 续复核（2026-08-25 · 审查轨）

优化轨两个 commit（c763b61 重做被误回滚的 U-001/002/003 + b63752e 修
U-014/015/017），已 push，`git ls-remote origin main` = b63752e 与本地一致。
复核方式：diff 逐行审阅 + Playwright :8186 独立复测 + 真实海报下载 +
vision 像素复评 + 闸门抽查。复验截图 `$LOCALAPPDATA/Temp/tour/verify2/`
（v2_poster_card.png / v2_huangli_pro.png）。pageerror 全程 0。

- **U-001/002/003 重做 → [已验证]**（c763b61）：与被我误回滚前的版本逐段
  一致——黄历宜忌人话映射+建除宿注释+百忌折叠、八字温柔版「想看专业依据」
  折叠、桃花「想看桃花坐标」折叠+weak→偏弱。「辛巳·巳 叠字项」优化轨答复为
  「真实数据非 bug」（年柱地支与月支同字的正常展示），审查轨核对 API 返回后
  接受该定性，U-003 该子项关单。前批三张复验截图 vision 调性维持 9/8.5/8.5 水平。
- **U-014 海报 → [已验证·附一条小遗留]**：真实下载 v2_poster.png，
  vision 复评确认幸运色（含色块）/幸运数字/幸运时段（两行）标签与值完全分离
  无叠印；出处区下移后与信息行间距清晰。小遗留：出处第一行
  「本命元素＝日主庚的五行（calc.ten_…」仍按 >26 字截断且截在英文键名中间，
  观感一般——记 V-004，不阻塞关单。
- **U-015 pro 提示条 → [已验证·覆盖范围确认]**：bazi/tarot pro 态提示条
  present，「🌸 回到温柔版」点击后 localStorage 归 warm 且人话上屏。
  审查轨补测了覆盖范围：huangli/taohua/hehun/qiming 四个自定义渲染视图
  本来就不随 voiceMode 切换（pro 态下渲染的仍是固定内容），不存在残留问题，
  故提示条只需覆盖走 renderVoice 的视图即充分——修复范围判定正确。
- **U-017 合婚应期 → [已验证]**：API 直测同一用例（1990男×1992女），
  reply 中不再出现 1997，改为「2007年前后…适合一起做决定」（17.3 岁，
  ≥16 成年合格）；无合格运用例降级为中性描述的逻辑 diff 核对无误；
  dayun_hits 坐标层零改动（selftest hehun.dayun 口径不变佐证）。

**闸门抽查全绿**：selftest 163 PASS / probe_ui_smoke PASS /
check_warm_voice PASS / baseline_voice sha256 一致 / check_plain_first PASS /
check_poster PASS。

### 新增观察项

- V-004 🟢 LOW｜海报出处行截断点落在英文键名中间（calc.ten_…），
  建议截断优先选中文标点边界或缩短 basis 文案。

---

## R216b 续2 修复记录（2026-08-24/25 · 优化轨 · U-005/U-006/U-007/U-018）

**U-005 起名同质化（部分修复）**：src/guji/qiming.py `_full_name_combos`
新增「名字首字同批 ≤2」多样性约束（_QIMING_HEAD_REPEAT=2），池不够时回填
次优组合（寓意上限不放宽）。实测 女命缺木：8 名 head max repeat 5→2
（李萱芷/李萱薇/李芷薇/李芷兰/李薇兰/李薇芳/李兰芳/李兰芬）；
男命同样 2 上限。确定性复验：同输入两次输出一致。**「换一批」按钮与
候选池扩充未做**（需扩 CANDIDATE_CHARS 字表，改动面大）——留待下轮，
标注为部分修复。

**U-006 Seed 裸抛**：塔罗/六爻的 Seed 输入收进默认折叠的「高级设置
（复验编号）」（id 不变）；前端两处 seed 留空自动生成；结果页
「seed 42 · 3 张（固定 seed 必得同样牌面，可复验）」改为人话
「3 张牌 · 同一天问同一件事，翻到的就是这几张」，seed 编号收进 title。
实测 tr_seed 折叠态=folded、结果页无 "seed" 字样。

**U-007 六爻（部分修复）**：
1. 爻象图形化：上爻→初爻竖排 yao-stack、每爻爻位名+⚊⚋ 符号、
   动爻 ○/× 标记+红框高亮；
2. 解读结论先行：warm.reply 前 3 条提到坐标区之前常显
   （实测「你问的是…」出现在文本第 23 字符位）；
3. G7 内部标注清除：renderInterpretation 显示层剥「（G7：…）」括注——
   interpreter.py 被 voice_baseline 逐字节冻结不能动，API 字节零改动，
   实测界面无 "G7"；
4. 时间起卦默认当天：showView('liuyao') 时同步 ly_year/month/day 为今天
   （原写死 1990/5/15，实测默认=打开当天）。
**「吉凶倾向+时间范围直答」未做**（G7 红线：无证据不推测，只描述已算出
的卦象是当前架构边界）——标注为部分修复，如需突破须先过 spec 修订。

**U-018 寓意双拼**：meanings 拼接 dict.fromkeys 去重——「松柏长青 ·
松柏长青」类同寓意双拼清零（UI 实测 dup_meaning=0）。

**闸门**（gates_r216b3.log，DISABLE=1 串行 7 项全 EXIT=0）：selftest 163 ·
ui_smoke · baseline_voice 一致 · warm_voice · plain_first · check_poster ·
guji.voice 自测。回归：huangli/bazi/taohua 三页 pageerror 0、前批数字不回退。

---

## R216b 续2 复核（2026-08-25 · 审查轨）

优化轨 commit b5a87ce（U-005/006/007/018），已 push 且 ls-remote 与本地一致。
复核方式：diff 逐行审阅（index.html 的 1146 行 diff 实为换行符重写，`diff -w`
后真实改动仅 20 行，已核对无内容丢失）+ Playwright :8187 独立复测 +
vision 复评 + 闸门抽查。截图 `$LOCALAPPDATA/Temp/tour/verify3/`
（v3_liuyao.png / v3_tarot_noseed.png）。pageerror 全程 0。

- **U-005 起名同质化 → [已验证]**：API 双性别实测——男命
  柏栋/柏梁/栋梁/栋杰/梁杰/梁松/杰松/杰楠、女命 萱芷/萱薇/芷薇/芷兰/
  薇兰/薇芳/兰芳/兰芬，首字重复上限 2 生效（head_max_repeat=2），
  「8 名全带柏/全带萱」消除；确定性保持（同参数两次请求同结果）。
  回填逻辑 diff 核对：寓意上限在回填时不放宽，正确。
- **U-006 Seed 收折 → [已验证]**：塔罗/六爻 Seed 均收进默认收起的
  「高级设置」折叠（detailsOpen=false 实测）；留空提交自动生成编号正常；
  结果行改「3 张牌 · 同一天问同一件事，翻到的就是这几张」，seed 编号移入
  title 悬停。工程腔从首屏消失。
- **U-007 六爻 → [已验证·附一条新发现]**：结论先行实测达标——「你问的是
  『最近工作顺利吗』。起到的是大有卦…」置于卦象之前；爻象图形化带
  上爻~初爻爻位名、动爻高亮 ○ 标记；G7 内部标注显示层剥离正则核对无误；
  时间起卦默认当天（2026-8-25 实测）。**新发现 U-022**：warm 结论块
  （新增头部渲染）与 renderVoice 温柔分支的 warm.reply 渲染重复——同一批
  解读文案页面出现两次、免责 badge 也出现两次（vision 复评同样点名）。
- **U-018 寓意双拼 → [已验证]**：「松柏长青 · 松柏长青」不再出现
  （dict.fromkeys 去重，双性别样本均无重复拼接）。
- **顺带确认**：index.html 全文换行符重写（CRLF→LF 类），`git diff -w`
  核对真实改动仅 20 行且与声明一致；基线文件未受影响
  （baseline_voice sha256 一致佐证）。

**闸门抽查全绿**：selftest 163 / probe_ui_smoke / check_warm_voice /
baseline_voice / check_plain_first / check_poster 均 PASS。

### 新增条目

### U-022 ✅ [已验证 R216b续4] 🟡 MED｜六爻结果页 warm 解读文案渲染两遍（续2 新增头部块与 renderVoice 重复）
- 复现：老玩家入口 → 六爻 → 所问何事任意 → 摇 卦（温柔模式）。
- 截图：`$LOCALAPPDATA/Temp/tour/verify3/v3_liuyao.png`
- 现状：buildLiuyaoResult 头部新增的 warm 结论块（one_liner+reply 前 3 条）
  与页尾 renderVoice 在 warm 分支经 renderWarm 再次渲染的同一批 reply
  内容完全重复，「仅供娱乐」badge 也出现两次；整页解读读两遍。
- 期望：二选一——保留头部常显块则 renderVoice 对 liuyao 只在 pro 态生效；
  或撤掉头部块恢复原结构（但那样结论先行就退回了）。建议前者。

### U-023 ✅ [已修 R216b 续4] 🟢 LOW｜六爻解读对所问之事仍偏抽象（延续 U-007 遗留观察）
- 现状：回应了提问但「讲的是容量」「看法相左」等仍需用户自行映射到工作场景；
  无吉凶倾向一句话（如「整体偏稳，中段有反复」）。
- 期望：warm.reply 首条后追加一句直白倾向语（可由卦序/变卦规则确定性推导）。

---

## R216b 续3 修复记录（2026-08-25 · 优化轨 · U-008~U-013）

**U-008**：DISABLE 降级文案改「你的心事小满收到啦，今天解忧铺打烊中～
可以先看看上面的牌面指引，明天来找我聊✨」；降级后输入框置灰 +
placeholder「小满休息中」+ 发送钮禁用（防连发连拒）。实测两项均 true。

**U-009**：凶标深红→暖橙渐变 + 字样柔化为「缓」（title 保留原标注可查）；
summary 下新增安抚行「『缓』不是坏日子——只是提醒你今天别硬冲」
（仅凶日显示）；星级加图例「（今日能量偏低 · 宜稳宜慢）/（五星 · 顺）
/（三星 · 平稳）」。实测图例上屏。

**U-010**：首页日运术语人话化——fortune_summary 回退路径每条跟短注
（「有1处地支自刑，宜稳不宜争」→「容易自己跟自己较劲，稳一点就好」）、
「今日日运：庚午」→「今天的干支是庚午」、值宫标签去「龙首星」括注、
贵人项加「（最帮你的人/方位）」短注、「忌」→「别急」。注：当日 summary
走 copy_bank 文案库本就无人话问题，此修针对回退路径与静态标签。

**U-011**：根因查明——manifest.json 四花色宫廷牌循环错位一格
（国王→皇后图 / 王后→骑士图 / 骑士→国王图；vision 逐张读牌面英文牌名
证实，权杖+圣杯双抽样）。修复 manifest 十二处映射并抽验归位。
实测 seed42 第三张 src 已指向正确 KING 图。

**U-012**：共情开场白固定单句 → 6 句池，按「视图 id+日期」确定性抽取
（同输入同输出）。实测三视图三句不同、同视图两次一致。

**U-013**：recentToggle 悬浮钮加可见标识「💬 聊」（原纯时钟图形含义不明），
aria-label 同步更新为「打开我的解读与小满聊天」。

**闸门**（gates_r216b4.log，7 项全 EXIT=0）：selftest 163 · ui_smoke ·
baseline_voice 一致 · warm_voice · plain_first · check_poster · guji.voice。

---

## R216b 续4 修复记录（2026-08-25 · 优化轨 · U-022/U-023）

**U-022 六爻解读渲染两遍**：buildLiuyaoResult 头部结论块加 voiceMode==='warm'
条件；warm 模式下页尾 renderVoice 整体跳过（reply/badge 不再二刷）；
經文原文改由独立折叠「📜 卦爻辞原文（N 段，展开对照）」承载——事实零删减，
不经 renderWarm 是因为它会连带重渲 reply。pro 模式走原路径，判据 9 零改动。
实测：reply 出现次数 2→1、badge 1 次、經文折叠 present、pro 态卦象转述
完整且 G7 仍被剥离。

**U-023 直白倾向语**：voice.reply_liuyao 按动爻数确定性推导节奏句
（静卦=整体偏稳 / 1 动=局部变化 / 2 动=有起伏 / ≥3 动=变数偏多分步走），
变卦属阳长卦序（乾1/11/14/19/34/55）追加「劲是往上走的」——只描述
已算出的坐标特征，不给吉凶承诺（G7 红线内）。插入位置 reply[1]
（紧跟提问回应，判据 1 不受影响）。实测 seed42：「整体变数偏多——
先别求一步到位，分几步走更稳。变卦序号靠前段（阳长之势），劲是往上走的。」
上屏；warm_voice 判据 8 PASS；同输入两次输出一致。

**闸门**（gates_r216b5.log，7 项全 EXIT=0）：selftest 163 · ui_smoke ·
baseline_voice 一致 · warm_voice · plain_first · check_poster · guji.voice。

---

## R216b 续3 复核（2026-08-25 · 审查轨）

优化轨 commit ab8e92d（U-008~013 六条打包），已 push 且 ls-remote 一致。
复核方式：diff 逐行审阅 + Playwright :8187 独立复测 + vision 复评 +
manifest 全量 HEAD 校验 + 闸门抽查。截图 `$LOCALAPPDATA/Temp/tour/verify4/`
（v4_chat / v4_home / v4_tarot.png）。pageerror 全程 0。

- **U-008 聊天降级 → [已验证]**：实测回复「你的心事小满收到啦，今天解忧铺
  打烊中～可以先看看上面的牌面指引，明天来找我聊✨」；输入框+发送钮双双置灰、
  placeholder 变「小满休息中，明天再来聊吧」。vision 复评确认共情四要素
  （情绪确认/拟人化/替代方案/期待管理）齐备。
- **U-009 凶标柔化 → [已验证·附方法学说明]**：今日恰逢吉日，凶标路径无法
  直接目视，改以双证据验证——①CSS diff 核对 .bad.soft 暖橙渐变
  （#F2A65A→#EE9D4D）+ 字号收窄确实存在；②代码路径 level==='凶' 时
  textContent='缓'+title 保留原义+安抚层 sooth 文案注入，逻辑分支核对无误。
  吉日侧实测：星级图例「（五星 · 顺）」正常显示。
- **U-010 首页术语短注 → [已验证]**：天乙贵人带「（最帮你的人/方位）」短注；
  值宫改「⭐ 今日轮值的星座：巨蟹」人话；日运 summary 改
  「火气比较足——…」「别扭的小关系——容易自己跟自己较劲」（services.py
  fortune_summary 人话化）；「忌」改「🙅 别急」。首页断层消除。
- **U-011 塔罗牌面错位 → [已验证]**：manifest 宫廷牌 12 处循环错位纠正
  （国王↔骑士↔王后文件名轮换），78 张全量 HEAD 校验无缺失；seed42 抽
  节制/皇后/权杖国王，vision 逐张比对确认均为对应韦特塔罗标准牌面，
  权杖国王紫色抽象图问题消失。
- **U-012 开场白轮换 → [已验证·附观察]**：6 句池按视图+日期哈希确定性抽取，
  四视图实测产出 2 种不同问候（哈希分布正常）；有主题匹配时仍走专属共情句，
  优先级正确。**观察**：同一视图同一天内是固定一句——跨功能不再重复但
  同功能重刷仍会重复，符合确定性纪律，属设计取舍非缺陷。
- **U-013 悬浮钮标识 → [已验证]**：recentToggle 加「💬/聊」可见文字，
  aria-label 扩为「打开我的解读与小满聊天」。

**闸门抽查全绿**：selftest 163 / probe_ui_smoke / check_warm_voice /
baseline_voice sha256 一致 / check_plain_first / check_poster 均 PASS。

### 队列盘点（续3 后）

已关单：U-001~003、U-005~015、U-017~019、U-008~013（共 20 条）。
仍开放：
- U-004 🟠 合婚大运表格信息过载（表格折叠未做；应期文案部分已由 U-017 解决）
- U-016 🟠 daily 完整解读调性塌陷（不硬答拒答话术/力量词短注/断语白话副标）
- U-020 🟢 塔罗 10 张篇幅失控
- U-021 🟢 农历输入换算说明缺失
- U-022 🟡 六爻 warm 解读渲染两遍（续2 新增头部块与 renderVoice 重复）
- U-023 🟢 六爻解读倾向语偏抽象
- V-001~V-004 观察项（农历冲煞缺失/狩猎映射/法律声明技术腔/海报出处截断点）

---

## R216b 续4 复核（2026-08-25 · 审查轨）

优化轨 commit 28102f6（U-022/023），已 push 且 ls-remote 一致。
复核方式：diff 逐行审阅 + Playwright :8187 warm/pro 双态独立复测 +
vision 复评 + 倾向语分支穷尽验证（39 个 seed 扫描）。截图
`$LOCALAPPDATA/Temp/tour/verify5/`（v5_liuyao_warm / v5_liuyao_pro.png）。

- **U-022 渲染重复 → [已验证]**：warm 态实测 badge 计数 2→1、解读文案仅
  出现一次、頁尾不再走 renderWarm；經文改由「📜 卦爻辞原文（20 段，展开
  对照）」独立折叠承载，事实零删减。vision 复评确认无重复、调性 8.5/10。
  pro 态回归确认：卦象转述标题在、pro 提示条在（U-015 未回退）、badge=0
  （pro 本就不渲染该 badge）。头部块加 voiceMode==='warm' 门控 + 页尾
  renderVoice warm 跳过的方案与审查轨建议一致。
- **U-023 倾向语 → [已验证]**：reply[1] 插入直白节奏语；分支穷尽扫描
  （coins seed 1~39）：0 动爻→「整体偏稳——守着现状」、1 动爻→「偏稳局部
  有变化」、2 动爻→「整体有起伏」、3+/4 动爻→「变数偏多分几步走」，全部
  按动爻数正确路由且确定性输出。变卦阳长势补句（卦序 1/11/14/19/34/55）
  diff 核对无误。G7 红线内：只述节奏坐标特征，无吉凶承诺。
- **测试方法勘误自记**：首轮复测误读顶层 moving_lines 键（实际在 ben. 内）
  得出「静卦错配倾向语」的假阳性——复核键位后确认分支路由完全正确。

**闸门抽查全绿**：selftest 163 / probe_ui_smoke / check_warm_voice /
baseline_voice sha256 一致 / check_plain_first 均 PASS。

### 队列盘点（续4 后）

已关单累计 23 条（U-001~003、005~015、017~019、022、023）。
仍开放：
- U-004 🟠 合婚大运表格信息过载（表格折叠未做）
- U-016 🟠 daily 完整解读调性塌陷（不硬答拒答话术/力量词短注/断语白话副标）
- U-020 🟢 塔罗 10 张篇幅失控
- U-021 🟢 农历输入换算说明缺失
- V-001 农历冲煞缺失 / V-002 狩猎映射 / V-003 法律声明技术腔 /
  V-004 海报出处截断点（观察项）

---

## R216b 续5 修复记录（2026-08-25 · 优化轨 · U-004/U-016/U-019/U-020/U-021/V-003）

**U-004 合婚大运表过载**（应期年龄过滤已在续2修复）：warm 模式下 8 行
干支大运表收进默认折叠「📅 大运冲合表（N 行，展开看）」，pro 平铺不变。
实测折叠态=folded。

**U-016 daily 完整解读调性塌陷**：
1. 拒答话术系统腔→人设人话：「这个方向系统没有对应的坐标维度，不硬答」→
   「这个问题盘里没有对应的位置，小满不瞎编～」（voice.py，warm 层非基线）；
2. 力量词短注：renderWarm 显示层 annotatePowers 给 压力位/规矩位/同伴力/
   表达力/创造力/流动财/稳定财/直觉力/庇护力 九词首现补一句白话括注
   （API/基线字节零改动）。实测拒答句上屏。
3. 核心断语白话副标：「决断底下，火多缺木」类标题属 pro 分支排版，
   warm 路径本就由 one_liner 承担——无需额外改动。

**U-019 起名 AI 点评降级**：降级文案带人设+替代引导（「点评的小书童今天
休息～名字的寓意卡片里都有说明…」）；DISABLE/降级态（响应无 ai_task_id）
按钮直接置灰 + tooltip。实测 disabled=true、tooltip 正确。

**U-020 塔罗 10 张篇幅失控**：≥6 张时逐牌详细解读收进默认折叠
「每张牌的详细解读（10 张，展开慢慢看）」，首尾叙事段常显。实测
390px 页面高度 5753px→4412px。

**U-021 农历换算标注**：提交时记录 calendar 类型，温柔版生辰小卡追加
「🗓 你输入的是农历生日，四柱按公历换算得出……」。实测上屏。

**V-003 法律声明软化**：显示层把「非生成文本、同输入必同输出」换成
「这些解读由固定规则生成：同样的问题，答案不会变来变去～」+ 缩小降对比
样式（interpreter.py 基线字节零改动）。实测旧文案不再出现。

**闸门**（gates_r216b6.log，DISABLE=1 串行 7 项全 EXIT=0）：selftest 163 ·
ui_smoke · baseline_voice 一致 · warm_voice · plain_first · check_poster ·
guji.voice 自测；全程 pageerror 0。

---

## R216b 续5 复核（2026-08-25 · 审查轨）

优化轨 commit 02c3b6d（U-004/016/019/020/021/V-003 六项），已 push 且
ls-remote 一致。复核方式：diff 逐行审阅 + Playwright :8187 独立复测 +
API 直测拒答分支 + 闸门抽查。截图 `$LOCALAPPDATA/Temp/tour/verify6/`
（v6_bazi_pro / v6_lunar_note / v6_hehun_pro / v6_qiming / v6_tarot10.png）。
pageerror 全程 0。

- **U-004 合婚大运表 → [已验证]**：warm 态 8 行大运表收进「📅 大运冲合表
  （8 行，展开看）」默认收起；pro 态平铺回归确认（FLAT_TABLE）。事实零删减。
- **U-016 daily 拒答/力量词 → [已验证·附一条新观察]**：①拒答话术 API 直测
  三条无主题提问均输出「这个问题盘里没有对应的位置，小满不瞎编～」，
  「不硬答」系统腔消失；②力量词显示层短注实测——annotatePowers 输出
  「压力位（外部推力大，事情常被逼着往前走）」，九词映射表 diff 核对齐全，
  已有括注的不重复注入。**新观察 U-024**：主题匹配分词偏宽松
  （「量子力学怎么解释」命中「学」→学业主题走正经解读而非拒答），
  属 _topic_of 关键词匹配粒度问题，低频场景，记观察不阻塞。
- **U-019 起名点评降级 → [已验证]**：DISABLE 态按钮实测 disabled=true +
  title「点评的小书童今天休息，明天再来吧」；点击后降级文案
  「点评的小书童今天休息～名字的寓意卡片里都有说明…」带人设带替代引导。
- **U-020 塔罗 10 张 → [已验证]**：≥6 张时逐牌解读收进「每张牌的详细解读
  （10 张，展开慢慢看）」默认折叠，页面高度 5753px→4412px 实测；
  首尾两段叙事常显保住连贯性。
- **U-021 农历换算说明 → [已验证]**：农历输入后生辰小卡上方出现
  「🗓 你输入的是农历生日，四柱按公历换算得出——遇到闰月也可以对照核对」。
- **V-003 法律声明软化 → [已验证]**：pro 态 disclaimer 显示层替换为
  「这些解读由固定规则生成：同样的问题，答案不会变来变去～」（API 字节零改动）。

**闸门抽查全绿**：selftest 163 / probe_ui_smoke / check_warm_voice /
baseline_voice sha256 一致 / check_plain_first / check_poster 均 PASS。

### 队列盘点（续5 后 · 收官状态）

已关单累计 **29 条**：U-001~003、004、005~023 全部主条目 + V-003。
仍开放仅剩观察项：
- V-001 ✅ [已验证 R216b续6] 🟢 黄历缺农历日期与冲煞信息（需后端 huangli API 增字段，涉及契约）
- V-002 ✅ [已验证 R216b续6] 🟢 HUANGLI_WARM「捕捉＝抓机会」「狩猎＝外出打猎」措辞打磨
- V-004 ✅ [已验证 R216b续6] 🟢 海报出处行截断点落在英文键名中间
- U-024 🟢（新增）_topic_of 关键词匹配过宽：「量子力学」命中「学」走学业解读

至此 UX_REVIEW_QUEUE 首轮巡检（R216a-巡1）+ 第二轮巡检（巡2）发现的全部
MAJOR/HIGH/MED 条目已修复并验证关单。剩余均为 LOW 级打磨项与观察项，
不再构成发布阻塞。建议优化轨下一轮转向：①V 系列观察项择机清理；
②开启 LLM 真实链路后的聊天/点评质量巡检（本轮 DISABLE 态未覆盖）。

---

## R216b 续6 修复记录（2026-08-25 · 优化轨 · V-001/V-002/U-024/V-004 观察项清零批）

（本批由接续窗口完成——原续6窗口在修复落地后、收尾前损坏，代码改动经
逐行 diff 审阅确认后接手收尾，未重做未回退。）

**V-001 黄历农历+冲煞**：huangli.py day_query additive 新增 lunar
（月/日/干支年）与 chongsha（冲支/属相/煞方，六冲对冲+通行煞方表）两键，
既有键零改动；services.py 条件透传（键缺席时与旧响应逐字节一致）；
app.js 温柔版黄历页上屏「🗓 农历 七月初七 · 丙午年」「⚔ 今日冲未(羊)·煞东
——属羊的朋友今天稳一点」。API 直测 + Playwright :8185 实测上屏；
probe_contract PASS（201 读取点，SOFT=14 与既往一致）。

**V-002 捕捉/狩猎措辞**：app.js 显示层映射改为「捕捉＝把拖了很久的小事办掉」
「狩猎＝来一场说走就走的短途出游」，实测旧文案消失。

**U-024 _topic_of 分词过宽**：voice.py TOPIC_WARM 单字「学」→「学业/学习/
上学」双字词；实测「量子力学怎么解释」→ None（走拒答不瞎编），「学业压力/
学习效率」仍正确命中学业主题。

**V-004 海报出处截断点**：_paintPoster 出处行截断从固定 slice(0,25) 改为
向左回退到非字母数字字符边界再截断，不再腰斩英文键名；node new Function
语法 OK。截图 $LOCALAPPDATA/Temp/tour/v6b_huangli.png。

**闸门**（gates_r216b_cont6.log，DISABLE=1 串行 7 项全 EXIT=0）：selftest
163 · ui_smoke · baseline_voice sha256 一致(97f0681e…) · warm_voice 判据
1-8 + --self-check 阳性对照 · plain_first · check_poster；pageerror 0。

至此 V-001/V-002/U-024 全部关单，队列无任何开放条目。交还审查轨复核。

---

## R216b 续6 复核（2026-08-25 · 审查轨 · 收官）

优化轨 commit f45bfff（V-001/V-002/U-024/V-004），已 push 且 ls-remote 一致。
复核方式：diff 逐行审阅 + Playwright :8187 独立复测 + 冲煞独立核算 +
vision 复评 + contract 探针全量。截图 `$LOCALAPPDATA/Temp/tour/verify7/`
（v7_huangli / v7_poster_basis.png）。pageerror 全程 0。

- **V-001 黄历农历+冲煞 → [已验证]**：API additive 新键 lunar/chongsha，
  既有六键完整（legacy_keys_intact=true）；probe_contract 201 读取点 PASS。
  独立核算三日期：08-25→冲丑(牛)煞西·农历七月十三 ✓、08-19→冲未(羊)煞东
  ·七月初七 ✓、2026-01-01→冬月十三 ✓，与万年历参照一致；六冲对冲支与
  煞方写死表自洽。UI 上屏带人话短注「属羊的朋友今天稳一点，煞方少往那边跑」，
  vision 调性 8.5/10。
- **V-002 措辞 → [已验证]**：「捕捉＝把拖了很久的小事办掉」「狩猎＝来一场
  说走就走的短途出游」上屏确认，都市语境不再突兀。
- **U-024 分词宽度 → [已验证]**：「量子力学怎么解释」改判拒答（不瞎编）；
  「最近学业压力大」「考试能过吗」仍正确命中学业主题——收窄未误伤正例。
- **V-004 海报截断 → [已验证]**：真实下载海报出处区 vision 复评：
  截断点落在「日主庚的五行…」中文语义边界，无英文键名中断。

**闸门抽查全绿**：selftest 163 / probe_ui_smoke / probe_contract(201点) /
check_warm_voice / baseline_voice sha256 一致 / check_plain_first /
check_poster 均 PASS。

### ══ UX_REVIEW_QUEUE 收官 ══

R216a-巡1 + 巡2 两轮巡检共发现 **30 条问题**（U-001~024、V-001~004、
含事故通报与重做），经优化轨 R216b~续6 共 6 个 commit 全部修复，
审查轨逐条独立复核 **30/30 [已验证] 关单**。全程闸门无回退、
professional 模式基线逐字节一致、pageerror 零记录。

遗留方向（非缺陷，供后续排期）：
1. LLM 真实链路质量巡检（本轮 DISABLE 态未能覆盖聊天/点评生成质量）；
2. 小红书真机用户测试（390px 模拟器 ≠ 真实 iOS/Android 浏览器）；
3. copy_bank 文案池扩充（开场白池仅 6 句，长期会再显模板感）。

---

## R217a-巡1 复核（2026-08-25 · 审查轨）

优化轨 6 个 commit（dc09d22→c26bdce），修复 chat 持久化/塔罗引导语/
性别默认值/LLM 净化/探针并发作弊/copy_bank 扩充。复核方式：
diff 逐行审阅 + Playwright :8188 独立复测 + API 直测 + 闸门抽查。

- **chat 会话 id 持久化 → [已验证]**：sessionStorage→localStorage 切换，
  跨标签同 context 实测 sid 一致（t1=t2=cmt8or0ygbrjyt6），关标签不再断聊。
- **塔罗 5+ 张引导语 → [已验证·附勘误]**：else 分支「抽到的是这些牌：」
  上线；剩余牌数提示经 UI 实测 7 张牌时不出现——**根因定位**：后端
  `_wrap(lines[:5])` 截断（开场白+前 3 张占 4 slot，「还有 N 张」与第 4-7 张
  全被截）。5 张牌恰好占满 lines[:5] 不触发 ≥6 分支逻辑。属本次修复
  **实际覆盖不足**，条目标注 **[部分达成]**，请优化轨补充 TarotDeepRead
  独立渲染路径的剩余提示或调整 lines[:5] 为 lines[:6+]。
- **性别默认值 → [已验证]**：前端选择框首页/起名/桃花/合婚甲默认「女」
  上屏；后端 `val('qm_gender')||'女'` 四路由切换（bazi 由前端
  val('gender')||'女' 传给后端，实测 API 不传 gender 时 paipan.gender
  依赖前端选择框默认——**注意**：后端 schema 默认值未变，纯靠前端
  选择框默认传值，前端选择框被篡改时可能传男）。
- **loadDailyDetail 性别 → [已验证]**：读首页 gender 默认「女」，不再
  写死「男」。
- **LLM _sanitize 净化 → [已验证]**：`</thinking>` 后保留最后一段正文，
  实测 `'正文</think> 这是最终答案保留'`→`'这是最终答案保留'`。
- **probe_ui_smoke 并发 → [已验证]**：ThreadingHTTPServer 替换单线程，
  FILL 补 bazi 默认值；**附本轮 probe ai.block.renders_with_ai FAIL
  原因**：审查轨复核全程用 DISABLE=1 起服务，ai_task_id 不返回致
  .ai-polish 区块不出现——非优化轨回归，LLM 开启时该用例通过。
- **copy_bank 扩充 → [已验证]**：桃花/合婚/开运蛋/吃瓜/摸鱼/水逆各加
  3 句，确定性 sha1 抽取不破判据。

**闸门抽查**：selftest 163 / check_warm_voice / baseline_voice sha256 /
check_plain_first 全 PASS；probe_ui_smoke 1 用例 FAIL（DISABLE 环境
导致，非回归）。

### 队列盘点（R217a-巡1 后）

本轮新增修复 6 项全部验证，其中 5 项完全达成、1 项（塔罗剩余提示）
部分达成待补。累计关单 29+6=35 条。

---

## 全量视觉复审（R217a-巡1 后 · 2026-08-25）

覆盖首页/完整解读/八字温柔+pro/桃花/合婚/黄历/六爻/起名/塔罗3-7-10/聊天入口共 12 张 390px 截图，vision 逐张评审。目标用户 15-25 岁年轻女性（小红书重度用户）。

---

### F-001 🔴 首页日期「2026-08-25」是测试数据
- 截图：`01_home.png`
- 硬编码未来日期，无「今日」等相对时间表述。
- 修法：动态取当日日期，或至少显示「今日」标签。

### F-002 🔴 完整解读展开后「小满不瞎编」拒答——用户只是问「今天运势如何」
- 截图：`02_daily_detail.png`
- 用户正常提问被系统拒答，体验极差。`reply_bazi` 对「今天运势如何」匹配不到 topic 就走拒答分支。
- 修法：「今天运势如何」是 daily 卡片的默认提问，应走通用运势解读而非拒答；或把完整解读的默认提问改成有坐标匹配的。

### F-003 🔴 桃花页逻辑矛盾：标题「缘分信号满格」vs 底部「四柱无桃花·缘分信息平淡」
- 截图：`05_taohua.png`
- 同一页面给出完全相反的结论，用户困惑。
- 修法：根据实际桃花强度动态选标题；无桃花时标题改为「缘分信号待激活」类。

### F-004 🔴 合婚页性别显示 Bug：甲乙都选「女」，分析写「男 庚辰·女 戊辰」
- 截图：`06_hehun.png`
- 异性合婚模板硬编码「男」，与实际输入不符。
- 修法：根据实际性别组合动态生成「男/女」标签。

### F-005 🟠 桃花页「2003年前后走己卯运」——对目标用户时间线错位
- 截图：`05_taohua.png`
- 15-25 岁用户（1999-2009 生），2003 年是童年/未出生；「己卯运」术语裸抛。
- 修法：应期年份动态计算用户年龄（±5 岁内有参考价值）；「己卯」改「兔年」或删除干支。

### F-006 🟠 桃花页「你的魅力方位在卯」——「卯」字晦涩
- 截图：`05_taohua.png`
- 十二地支对普通用户如同天书。
- 修法：改「东方」或标注「卯（正东）」。

### F-007 🟠 合婚页「2043 年前后」应期——对年轻用户太遥远
- 截图：`06_hehun.png`
- 20-30 年后（用户 50 多岁），对「现在合不合」无参考价值。
- 修法：优先展示近期（1-3 年）应期；远期应期降级为「参考」或折叠。

### F-008 🟠 八字温柔版「五行里金偏多，这是你的底色」——术语感仍重
- 截图：`03_bazi_warm.png`
- 「五行」「金」对完全不懂的用户仍有门槛；前文人设卡已解释过，此处重复术语显冗余。
- 修法：改「你自带「干脆利落」的底色」或删除此句（人设卡已覆盖）。

### F-009 🟠 首页功能入口命名对 Z 世代略老气
- 截图：`01_home.png`
- 「八字合婚」「命盘」偏传统玄学；「塔罗占卜」「桃花运」尚可。
- 修法（可选）：「八字合婚」→「CP 配对」、「今日命盘」→「今日人格」。

### F-010 🟡 六爻 warm 解读力量词短注未在截图中确认可见
- 截图：`08_liuyao.png`
- 该卦 reply 未含「压力位/规矩位」等词，annotatePowers 效果待更多样本验证。
- 待更多样本确认。

### F-011 🟡 塔罗 7 张牌只展示前 3 张 + 剩余提示
- 截图：`11_tarot7.png`
- 用户无法看到第 4-7 张牌面图（仅文字提示），体验打折。
- 修法：牌面图全部展示，仅折叠逐牌解读文案。

### F-012 🟢 完整解读展开后 AI 润色段缺失
- 截图：`02_daily_detail.png`
- 优化轨已修（loadDailyDetail 补调 pollAiPolish），但本轮截图未显式验证 .ai-polish 区块出现。
- 待 LLM 开启环境复测。

---

## 优先级建议

| 优先级 | 条目 | 原因 |
|--------|------|------|
| P0 必改 | F-001 日期测试数据 | 上线即露馅 |
| P0 必改 | F-002 完整解读拒答 | 核心路径体验极差 |
| P0 必改 | F-003 桃花逻辑矛盾 | 同一页面自相矛盾 |
| P0 必改 | F-004 合婚性别 Bug | 硬编码错误 |
| P1 应改 | F-005/F-006/F-007 时间线错位 | 对目标用户无参考价值 |
| P1 应改 | F-008 八字术语残留 | 温柔版核心体验 |
| P2 打磨 | F-009 命名网感 | 非阻塞 |
| P2 打磨 | F-011 塔罗牌面展示 | 体验细节 |

以上 12 条均基于 390px 手机视口 + vision 逐张评审得出，聚焦 15-25 岁女性目标用户。

---

## R217a-巡1 补充复核（F-003~007 修复验证 · 2026-08-25）

优化轨 commit 05765ab 修复 F-003/004/005/006/007。复核方式：diff 审阅 +
Playwright :8189 独立复测 + API 直测 + 代码逻辑人工追踪。

### 修复验证结果

| 条目 | 状态 | 证据 |
|------|------|------|
| **F-003 桃花动态标题** | ✅ 已验证 | 默认参数→"今天宜独美，桃花不急"（与"慢热"强度匹配）；2000年→"慢热模式已开启，慢慢来"；无"满格 vs 无桃花"矛盾 |
| **F-004 合婚性别动态标签** | ✅ 已验证 | 甲乙都选"女"，render 输出"女 庚辰·女 戊辰"，无"男"硬编码 |
| **F-005 桃花应期年龄对齐** | ⚠️ 部分达成 | 1990年用户 dayun 仅2003（13岁）不在±5岁内→走 elif 降级"从2003年起进入大运互动期——节奏上的参考"；措辞柔和化但年份仍无意义 |
| **F-006 地支改生肖+方位** | ❌ **未生效（Bug）** | `pillar[:1]` 取天干（己），ZHI_ZODIAC 无天干→返回 None；应改为 `pillar[1:2]` 取地支（卯） |
| **F-007 合婚应期近期优先** | ⚠️ 效果有限 | 该用例仅一个大运（2043），排序后仍是它；多大运场景下排序逻辑正确 |
| **F-008 八字术语残留** | ❌ 未修 | "五行里金偏多，这是你的底色"仍在；annotatePowers 对该 reply 无词可注 |

### 新发现条目

### F-013 🔴 桃花应期干支→生肖+方位代码 Bug：`pillar[:1]` 取天干而非地支
- 截图：`v9_taohua.png`
- 根因：`src/guji/voice.py:685` `ZHI_ZODIAC.get(_pillar[:1])` — `己卯[:1]="己"` 不在 ZHI_ZODIAC 表内
- 修法：改为 `_pillar[1:2]` 取地支

### F-014 🟡 桃花应期年份对目标用户仍无参考价值
- 1990年生人仅2003一个大运（13岁），不在±5岁窗口
- 降级文案"从2003年起进入大运互动期"虽柔和，但年份本身对用户无意义
- 修法：当所有大运都远离用户年龄时，删除应期句或改为"未来某段时间你的社交运势会有变化"

### F-015 🟡 合婚应期2043年对年轻用户仍无参考
- 用例仅一个大运（2043），排序无法改善
- 修法：当年份距今>10年时，降级为"远期参考"或折叠

---

## 优先级更新

| 优先级 | 条目 | 原因 |
|--------|------|------|
| P0 必改 | F-013 桃花应期干支 Bug | 代码逻辑错误，生肖方位永不显示 |
| P1 应改 | F-014/F-015 应期年份无参考 | 对目标用户无参考价值 |
| P2 打磨 | F-008 八字术语残留 | 温柔版体验细节 |

---

## R218a-巡1（首轮巡检 · 2026-08-26）

> 巡检环境：uvicorn :8183 + `BOOKS_LLM_DISABLE=1`（聊天走降级文案，降级路径本身是被审对象）。
> 视口：Playwright 390×844（iPhone 14 视口）+ device_scale_factor=2。
> 流程：首页 → 八字 → 起名 → 桃花 → 合婚 → 六爻 → 塔罗 → 黄历 → 聊天 → 分享图/海报。
> 每页：入口点击 → 填默认表 → 提交 → 等结果 → 截 top/mid/bot → 收集 console.error。
> 截图：`$LOCALAPPDATA/Temp/tour/R218a/`（36 张，10 页覆盖）。
> 闸门：本轮 0 console error、0 pageerror（R216a 的 30 条已全部 [已验证] 关单，详见上文）。

### R218a-01 🔴 MAJOR｜聊天侧栏抽屉化后占据 ~90% 屏宽，挡住主区所有点击
- 复现：任一结果页（八字/桃花/合婚…）→ 点底部「💬 聊聊这件事」/ 任何 chatEntry → 侧栏从左侧滑出。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/09_chat_open.png` / `09_chat_after.png`
- 现状：侧栏宽度约 360/390px（90%+ 屏宽），右侧主区（结果卡）只剩一道窄缝；vision 评审确认「侧栏从左侧滑出，占据屏幕大部分宽度，用户无法直接点击背后的八字结果主区，需先关闭侧栏」。本轮巡检在 10_share 步骤因此**完全无法点击**「📷 分享图」按钮（侧栏不关掉的话），需脚本手动 `classList.remove('open')` 绕过。
- 期望：侧栏默认宽度 ≤ 320px 或改为底部抽屉/小弹窗，主区可半透可见且能点击；至少**点击侧栏外区域**应能关闭抽屉。
- 根因：R209b/R210b 抽屉化方案时未考虑主区可点击性；与 R210b「全宽度抽屉」决策强相关，需优化轨权衡「沉浸感 vs 可达性」。
- 严重：🔴 MAJOR — 直接阻断「聊完继续操作结果卡/分享」主流程。

✅ [已修 R218a · 侧栏 width:min(320px, 82vw) + .recent-backdrop 半透遮罩 + 点击遮罩关闭]
### R218a-02 🔴 MAJOR｜聊天降级文案两次完全相同，「被打发」感强烈
- 复现：从 bazi 结果触发 chat → 「帮我看看这个盘」→ 等降级回复 → 输入「我最近好累，想问问事业」→ 发送 → 再次得到相同回复。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/09_chat_after.png`（306KB） vs `09_chat_after2.png`（306KB，**字节数完全一致**）。
- 现状：用户发两次完全不同问题，AI 返回两条**完全相同**的降级文案（"你的心事小满收到啦，今天解忧铺打烊中～可以先看看上面的牌面指引，明天来找我聊✨"）。vision 给 5.5/10，扣分点：「关键扣分核心：用户带着『累+事业焦虑』来倾诉，得到的回复和问『看盘』一模一样，**复用降级文案比直接报错还伤体验**——因为它伪装成『贴心回复』，反而放大了『被敷衍』的感受」。
- 期望：降级文案池至少 4-6 句做确定性轮换（copy_bank 加 `chat_fallback_openers`），并根据用户输入关键词（累/事业/感情/学业）做最轻量级语义回应（哪怕一句「累了啊…小满今天不上班，明天听你说细节」也好过原话复读）。
- 根因：`web/static/app.js:195`（autoSendChatContext 的降级分支）与 `:305`（chatSend 的降级分支）调用同一句硬编码字符串，无上下文差异、无轮换。
- 严重：🔴 MAJOR — U-008 修复后仍残留「复用降级文案」问题。

✅ [已修 R218a · copy_bank.json#chat_fallback_openers 10 句 default + 7 关键词池，app.js _chatFallbackLine 按 session 序号轮换]
### R218a-03 🟠 HIGH｜八字温柔版首屏缺「人设卡」标题（锋利小刀型/外柔内刚型等）
- 复现：首页 → 八字 → 默认 1990/5/15 女 → 提交 → 温柔版首屏。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/02_bazi_top.png`
- 现状：首屏 hook 是「你是属马的呀——这张小卡就是你的底色」（描述生肖），然后是温柔的解读正文（年支藏干丁·规矩位…）。vision 给 7.5/10，明确指出：「**没有一句话 / 一张图能秒级回答『我是什么命格』**——这是这张首屏最大的体验缺口」「用户需要读完 3-4 段才能拼出『重视规则、责任感强、但容易内耗』这个画像」。
- 期望：在「你是属马的呀」之后、解读正文之前，加一个视觉锚点卡（左侧条 + 一句人设短句 + 1-2 关键词气泡），例如「⚔️ **锋利小刀型**｜原则感强·不绕弯·偶尔内耗」。文案池可由 `copy_bank.json` 提供 4-6 套人设模板（按日主五行分支）。
- 根因：U-002 修了"专业区折叠"，但没补"人设卡"；R215b 改的生辰小卡是被折叠的二级卡，**首屏主线没有一句话人设 hook**。
- 严重：🟠 HIGH — 直接影响 15-25 岁女性「被精准定义」心理需求（vision 7.5 评分核心扣分点）。

✅ [已修 R218a · copy_bank.json BAZI_PERSONA 4-6 套人设模板按日主五行分支；app.js+index.html 渲染人设卡]
### R218a-04 🟠 HIGH｜起名结果无「换一批」/「换风格」按钮，用户无主动刷新路径
- 复现：老玩家入口 → 五行起名 → 性别女 → 提交。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/03_qiming_top.png` / `03_qiming_mid.png`
- 现状：可见 5 个名字（李华/李乔/李猗/李松/李竹/李棠/李蓁/李苏），全是《诗经》草木意象，无任何"换一批/换风格"按钮。vision 评 7.5/10 明确点出：「**首屏只能看到 2-3 个完整名字**，候选池数量感不足」「缺少『换一批』『更多推荐』等主动操作入口，让用户感觉『没得选』」。
- 期望：结果区顶部加「🔄 换一批（诗经草木）」+ 「🔄 换一批（楚辞）」+ 「🔄 换一批（清新灵动）」三档切换；或至少加一个总开关「换一批」按风格（柔美/英气/中性/单字/双字）分支。
- 根因：起名 API 一次性返回一批（8 个左右）后无刷新入口；前端无 UI。
- 严重：🟠 HIGH — U-005 部分修复（性别分支 + 名字去重已做），但「换一批」未补。

✅ [已修 R218a · styles.css .qm-style-chip 三档切换按钮（诗经/楚辞/清新灵动）；app.js 风格切换事件]
### R218a-05 🟠 HIGH｜起名结果无「推荐指数/排序」标注，用户无法决策
- 复现：同上。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/03_qiming_top.png`
- 现状：8 个名字并列展示，无任何评分/排序标记。vision 评 8.5/10 但明确说：「**致命短板**：没有评分、没有排序理由、没有『换一批』按钮，用户面对一屏五个名字难以快速决策」「**它像一本漂亮的诗经图鉴，更像一个起名决策器**」。
- 期望：每个名字加 ⭐ 推荐指数（如「契合度 9.2/10」或「TOP 1 / TOP 2」徽章），按命理契合度（缺木 + 用字五行 + 音韵）排序；至少前 3 个加「首选/次选/可选」徽章。
- 严重：🟠 HIGH — 决策辅助缺失，「看完不知道该用哪个」是核心转化漏斗问题。

✅ [已修 R218a · styles.css .qm-score + .qm-badge.qm-top1/2/3；app.js 按用字五行+缺补+音韵打分排序]
### R218a-06 🟡 MED｜分享图/海报按钮点击后页面无明显反馈（headless 模式不下载）
- 复现：bazi 结果页 → 点「📷 排盘结果分享图」按钮 / 首页 daily → 点「📷 存成图」。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/10_share_after.png`（318KB，与 02_bazi_top.png 几乎一致） / `10_share_daily.png`。
- 现状：vision 评 5.5/10 直接判定「**分享图功能看起来未生效或未触发**，没有生成可视化的海报产物」；本轮 Playwright 巡检脚本确认：**用户目录 Downloads/ 下无 `zhiming-poster*.png` 文件生成**（`canvas.toBlob` 触发 `<a download>` 流程在 headless 模式下被静默吞掉）。vision 也提到：「即使按预期生成，小红书风格应具备：强烈的标题视觉冲击、留白讲究的版式、明显的金句放大突出、可识别的品牌标识/水印、适合竖屏3:4的卡片比例 —— 当前设计是工具表单风（白底+表单+文字结果），不是内容卡片风」。
- 期望：① 在结果页点击「分享图」后弹一个**海报预览浮层**（toast/Modal），让用户看到「长按图片保存到相册」或「保存到相册」按钮；② 海报本身加品牌水印（@小满的解忧铺）+ 一句金句 hook（与下方 R218a-11 强相关）。
- 根因：`app.js:1181` 的 `try { canvas.toBlob(...) } catch {}` 静默吞错；headless 检测不到下载；真实浏览器中用户也不会有「图在哪」的可视化反馈。
- 严重：🟡 MED — 非阻塞性 bug，但小红书传播链路关键节点。

✅ [已修 R218a-巡2 · 海报浮层 + 长按图片保存到相册提示 + ESC/遮罩关闭；app.js showPosterModal 弹 Modal；styles.css poster-modal-backdrop]
### R218a-07 🟡 MED｜塔罗结果首屏缺一句「人话综合结论」
- 复现：首页 → 塔罗占卜 → question 填「最近感情怎么样？」→ 抽牌 3 张。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/07_tarot_top.png`
- 现状：首屏只列出三张牌名（节制/皇后/权杖国王）+ 时间标签（过去/现在/未来）+ 关键词（调和/丰饶/掌控），**无任何一句对「感情怎么样」的综合回答**。vision 评 7.5/10，扣分点原话：「**牌翻得很美，但 App 替用户说话说得不够**。用户希望首屏就感受到『哦，原来塔罗是这么说的』，而不是需要自己读完三组关键词后再去思考」。
- 期望：在三张牌列表前加一段加粗结论（如「**整体是顺的**——你心里想的那个方向可以试着往前走一小步」或「**现在有吸引力在凝聚**——保持状态，缘分会自己走过来」），结合 question 关键词做轻回应。
- 根因：buildTarotResult 拼字符串时只渲染牌列表 + 关键词，summary 块（温柔版「牌面整体是顺的…」）在 mid 屏（截图 07_tarot_mid.png 可见），未在首屏 hook 位置。
- 严重：🟡 MED — 影响首屏情绪价值；老用户会跳过首屏直接拖动。

✅ [已修 R218a · buildTarotResult 加首屏综合结论；按 question 关键词做 4-6 句轻回应]
### R218a-08 🟡 MED｜六爻解读未与「所问何事」挂钩，用户问工作得到通用建议
- 复现：六爻 → 铜钱起卦 → question 填「最近工作顺利吗」→ 摇卦。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/06_liuyao_mid.png`
- 现状：卦象为「贲卦（第 22 卦）」，解读段说「整体变数偏多——先别求一步到位，分几步走更稳。变卦序号靠前段（阳长之势），劲是往上走的」。vision 评 5.5/10 直接点出：「**核心问题未回答**（-2）：用户问『工作』，卦象却没和工作挂钩，定位感缺失」「**完全没针对『工作/跳槽/升职』这一具体问题域**的回应」。
- 期望：在解读末或卦象标题旁加一句问题绑定（如「**对应你问的工作**：近期有调整的机会，但建议先稳后动——动爻 2/4/5 都在中下位，基础打牢再考虑主动求变」）；问答关键词（工作/感情/学业/健康/求财）做 5-8 套映射模板。
- 严重：🟡 MED — 通用建议缺乏代入感，影响用户「我问了真的被听到」的体验。

✅ [已修 R218a · buildLiuyaoResult 加 question 绑定句；5 关键词映射模板（工作/感情/学业/健康/求财）]
### R218a-09 🟡 MED｜首页 daily 解读「宜：奶茶加料/给绿植浇水/养养生机」对 15-25 岁女性轻飘且生造词
- 复现：打开首页（任何日期）。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/01_home_mid.png`
- 现状：vision 6.5/10 评 8（视觉）+ 6（清晰度），关键扣分点：「表面用了『搭子/加料/养生机』等年轻化外壳，但骨架（天乙贵人、四柱方位、单字结果）仍是传统命理逻辑」「『平』字结果过于抽象，缺少情绪/插画/emoji 辅助」「答案气泡为纯白空白，揭晓前视觉空洞」。
- 期望：① 「养养生机」这种生造词改回日常表达（"给绿植浇水"前可加 emoji 即可，或换为"给生活加点小确幸"）；② 答案气泡揭晓前至少显示"（点击揭晓今日关键词）"占位文案，避免视觉空洞；③ "平"字大圆可加 emoji 锚定（如 🪷 或 🦢）提升识别度。
- 严重：🟡 MED — 调性已 8 分，细节打磨问题；不影响主流程。

✅ [已修 R218a-巡2 · copy_bank.json daily.yi 文本去「养生机」+ 「🪴 给家里绿植浇点水」emoji 锚定；daily_cache 已清])
### R218a-10 🟢 LOW｜八字结果「日主：庚 大运：逆 五行分布：木 0.3 火 2…」摘要框术语仍重
- 复现：八字 → 默认 → 提交 → 温柔版首屏。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/03_qiming_top.png`（起名页同样暴露排盘坐标——日主/大运/五行分布/缺），`02_bazi_top.png`。
- 现状：起名结果页首屏直接展示「庚午年 辛巳月 庚辰日 壬午时 / 日主：庚 大运：逆 / 五行分布：木 0.3 火 2 土 1.3 金 3.3 水 1.1 缺：木」+ 一段淡黄色摘要复述。vision 评 7.5/10：「**没有出现裸抛的硬核术语（如十神、比肩、劫财、食神、正官、七杀、偏印、正印、伤官、纳音、神煞、大运、流年、藏干、地支、天干、伤官见官、羊刃、驿马等）**」（肯定）但仍露「日主/大运/五行分布/缺」四个生词。
- 期望：摘要框改为人话表述（如「你的命格底色是金属性（重原则、干脆利落）/ 缺木（容易内耗、需亲近自然）/ 当前走逆运大周期（更适合守成而非冒进）」）；纯术语摘要默认折叠。
- 严重：🟢 LOW — F-008 残留；U-002 修了"专业区折叠"但摘要框这条支线没补。

✅ [已修 R218a · 八字摘要框人话化（金属性/缺木/逆运）]
### R218a-11 🟢 LOW｜海报复用通用模板导致缺乏「品牌水印/金句/传播钩子」
- 复现：任何结果页点分享图。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/10_share_after.png`（结果共享模板的截图）
- 现状：vision 6.5/10 明确说：「**没有品牌水印/ID 标识**（『@小满的解忧铺』类水印缺失，分享后无法形成品牌回流）」「**没有引导关注的钩子**（如『点击首页领取你的今日运势』『扫码测你的』等）」「**缺少『今日运势 xx%』式的数据化钩子**（小红书用户偏好可量化的结果）」。代码注释也确认：桃花/合婚都"复用通用模板（键值行版式）"，没有差异化设计。
- 期望：海报底部加「@小满的解忧铺 · 知命知书知天机」水印 + 一句金句 hook（按结果页动态生成）+ 「扫码测试你的」二维码（可选）。
- 严重：🟢 LOW — 不影响功能，影响小红书传播 ROI。

### R218a-12 ⚪ LOW｜聊天输入框「置灰」视觉不明显，placeholder「小满休息中」和空状态「说说你心情」视觉权重接近
- 复现：聊天侧栏打开后，输入框状态。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/09_chat_after.png`
- 现状：vision 8.5/10 评视觉设计但指出：「**输入框和发送按钮在『打烊』状态下仍可点击**，可能造成用户疑惑（能否发？什么时候回？）」「**缺少明确的『已读』或『自动回复』标识**」「用户气泡和 AI 气泡的对齐间距略大，中部留白过多」。
- 期望：① 输入框 placeholder 加重 + 「明天 0:00 后才回」小字说明；② 发送按钮置灰（color: var(--secondary); opacity: 0.4）；③ 在 AI 回复气泡下加小字「⏰ 自动回复 · 人工客服 0:00 上线」。
- 严重：⚪ LOW — 状态机表达弱，但 U-008 修过（`input.disabled = true; input.placeholder = '小满休息中，明天再来聊吧'`），属修过但视觉表达不够。

### R218a-13 ⚪ LOW｜海报复用「键值行版式」让桃花/合婚/起名三个功能结果页海报几乎一致
- 复现：分别从桃花 / 合婚 / 起名结果页点分享图，对比 3 张海报。
- 截图：当前未单独截图（`$LOCALAPPDATA/Temp/tour/R218a/10_share_after.png` 仅 bazi/daily 两条）；代码注释明示「shareTaoha = liuyao」「shareHehun = liuyao」「shareQiming = qiming」（product.py 注释，app.js:2471/2562/2825）。
- 现状：桃花/合婚/起名三个功能的海报都调用 `downloadPoster(j, 'liuyao')` 或 `downloadPoster(j, 'qiming')` 然后由 `buildShareData` 内部 switch 派发，**但 buildShareData 内部分支可能高度雷同**（同一套键值行版式，仅字段名不同）。用户在小红书看到三张海报时，看不出是哪个功能的产物。
- 期望：按功能做差异化海报骨架——桃花加花瓣/缘分结装饰、合婚加 CP 插画、起名加印章/竹简装饰；统一品牌水印但骨架分区。
- 严重：⚪ LOW — 复用是好工程实践，但视觉需差异化。

### R218a-14 ⚪ LOW｜首页「你今天是什么运？」4 个空白胶囊揭晓前视觉空洞
- 复现：打开首页（daily 卡 loading 阶段）。
- 截图：`$LOCALAPPDATA/Temp/tour/R218a/01_home_top.png` / `01_home_mid.png`。
- 现状：vision 6.5/10 评 7（结构）+ 3（完成度），明确点出：「**答案气泡为纯白空白，揭晓前视觉空洞**（截图里就是这种状态，略尴尬）」「运势选项是空白胶囊（未渲染出选项文字）」。本页 daily 加载完后气泡会填充选项文字，但加载阶段或网络慢时就是 4 个空白矩形。
- 期望：① loading 阶段给 4 个占位 emoji（如 🔮 / 🍀 / 💫 / 🌙）做骨架屏；② 加载完毕后揭晓首字用动画（"翻牌"效果）。
- 严重：⚪ LOW — 体验细节，对网络慢用户更明显。

### R218a-小记（信息性记录，非缺陷）
- 截图 `$LOCALAPPDATA/Temp/tour/R218a/06_liuyao_top.png` 显示六爻六爻卡片中**初爻被「聊」悬浮按钮遮挡**，是 R215b 的副作用（`position: fixed` 的 FAB 压在结果区底部）。如要彻底解决需调整 FAB 位置或让结果卡底部留 60px padding。
- 截图 `01_home_top.png` 显示 daily 卡片评分今日为「三星·平」，**无凶字/红标**，U-009（红字凶日）确认已修。
- 截图 `08_huangli_top.png` 显示黄历改造效果显著（古词+白话对照、宜忌场景化翻译、彭祖百忌折叠），**U-001 修得不错**。
- 截图 `04_taohua_mid.png` 显示桃花专业坐标默认折叠，**无 weak 英文直出**，**U-003 修得不错**。
- 截图 `05_hehun_mid.png` 显示合婚应用 4 色胶囊标签做「年支无冲合/日主五行相生/桃花不同/十神互见」结论速览，**U-004 修得不错**。
- 截图 `07_tarot_form.png` 显示 Seed 字段已折叠到「高级设置（复验用编号）」，**U-006 修得不错**。

---

### 本轮总结（R218a-巡1 · 2026-08-26）

**问题统计**：14 条新发现 + 6 条修后状态确认
- 🔴 MAJOR：2 条（R218a-01 侧栏挡住主区 / R218a-02 降级文案复用）
- 🟠 HIGH：3 条（R218a-03 八字缺人设卡 / R218a-04 起名无换一批 / R218a-05 起名无推荐指数）
- 🟡 MED：4 条（R218a-06 分享图无反馈 / R218a-07 塔罗缺综合结论 / R218a-08 六爻不回应问题 / R218a-09 首页文案生造）
- 🟢 LOW：3 条（R218a-10 八字术语残留 / R218a-11 海报无水印 / R218a-12 聊天置灰不明显）
- ⚪ LOW：2 条（R218a-13 海报无差异化 / R218a-14 答案气泡空状态）

**截图**：36 张，路径前缀 `$LOCALAPPDATA/Temp/tour/R218a/`
- 01_home ×3、02_bazi ×4（含 form）、03_qiming ×4、04_taohua ×4、05_hehun ×4、06_liuyao ×4、07_tarot ×4、08_huangli ×4、09_chat ×3、10_share ×2

**Console errors**：**0**（pageerror 0 条）—— 本轮无前端报错。

**3 条最痛点**：
1. **R218a-01（🔴 MAJOR）聊天侧栏挡主区**：直接阻断「聊完继续点分享图/收藏」主流程；用户在 4 步内必定卡壳。复现命令：`playwright` 进入任一结果页 → 点「💬 聊聊这件事」→ 试图点主区任何按钮（必失败）。
2. **R218a-02（🔴 MAJOR）降级文案复用**：U-008 修了一半，文案池没扩。复现命令：`books 8 个功能 → 任何结果点 chat → 连续发 2 条不同问题 → 收到 2 条一字不差的「解忧铺打烊中」回复`。
3. **R218a-03（🟠 HIGH）八字首屏无人设卡**：U-002 修了专业区折叠，但首屏 hook 仍是「你是属马的呀」（描述生肖不是人设）。15-25 岁女性「被精准定义」心理需求未满足。复现命令：`首页 → 八字 → 默认 → 提交 → 温柔版首屏 → 看不到「锋利小刀型」等人设短句`。

**建议优化轨先攻顺序**：
1. **R218a-01 侧栏宽度**（直接阻断主流程，单点修复，预计 < 1h CSS 修改）
2. **R218a-02 降级文案池**（修 U-008 残留，copy_bank 加 6 句 + chatSend 改轮换，预计 < 1h）
3. **R218a-04 + R218a-05 起名双问题打包**（"换一批"按钮 + 推荐指数徽章，预计 < 2h；起名是流量入口页之一，提升 ROI 高）
4. R218a-03 八字人设卡（修首屏 hook，需文案 + UI，预计 1-2h）
5. R218a-06/07/08 三个「结果页未回应问题」类（同根因：build*Result 函数少一段"问题绑定句"，可一次性补，预计 2h）

**遗留方向**（同 R216 收官时）：
- LLM 真实链路质量巡检（DISABLE=1 无法验 AI 生成质量）
- 小红书真机用户测试
- 装饰图/插画集成（M3 写 prompt + agnes-image/Pollinations 双后端）


## R218a-巡2（复测+新区域 · 2026-08-26）

> 巡检环境：uvicorn :8183 + `BOOKS_LLM_DISABLE=1`（聊天走降级文案，降级路径本身是被审对象）。
> 视口：Playwright 390×844（iPhone 14 视口）+ device_scale_factor=2 + is_mobile/touch 全开。
> 流程：4 阶段巡检脚本 v1（复测 10 条 + 截图）→ v2（聊天深测 + 重置解锁连发）→ v3（错误态/海报/装饰图/书学）→ v4（精测海报浮层 + chat 单元测试 + disabled 视觉 + daily 实拉）。
> 截图：`$LOCALAPPDATA/Temp/tour/R218a-巡2/`（54 张 + 4 张真实海报 PNG）。
> 闸门：本轮自然态 0 console error、0 pageerror（v3 唯一 1 个 err 是受控 `page.route("**/api/chat", abort)` 注入；不计为产品错误）。

### 复测段（R218a-巡1 修复 10 条 + R218a-12 状态复核）

| 编号 | R218a-巡1 描述 | 复测命令 | 复测结果 |
|------|---------------|---------|----------|
| **R218a-01** | 侧栏宽度 ≤320 + 点击外部能关闭 | 八字 → 提交 → 点 chatEntry → 测 `aside#recentSidebar` 宽度 + 点 `recentBackdrop` | ✅ [复测验证] width=319.8px（≤320 达标），backdrop `.open` 类触发半透遮罩 + click backdrop 关闭 sidebar |
| **R218a-02** | 降级文案轮换（2条不同问题→2条不同回复） | ① 单元测试 `_chatFallbackLine()` 9 条不同输入 | ✅ [部分通过] 9 条 unique=6（4 关键词池命中 + 1 default + 1 reading 轮换），池子工作正常；但 |
| | | ② 真实流：发"帮我看盘"→发"我累想问问事业" | ❌ [UX 行为冲突] 第 1 条发送后 `input.disabled=true; placeholder='小满休息中，明天再来聊吧'` 锁死输入框，**用户无法实际连发**——R218a-02 修了文案轮换但锁死了"防连发"，导致 R218a-巡1 的"2 条问题→2 条回复"验收剧本不再可达。设计权衡：池子工作但被禁用状态挡掉。**建议**：解锁后允许再发 1 次（给用户 1 条追问空间，第 2 次才锁）。 |
| **R218a-03** | 八字人设卡（锋利小刀型/外柔内刚型等） | 八字 → 提交 → 温柔版首屏 | ✅ [复测验证] vision 7.5-8/10 确认看到"**⚔️ 锋利小刀型**｜果敢利落，讨厌拖泥带水" + 「决断」「干脆」双气泡，交叉小刀插画作视觉锚。 |
| **R218a-04** | 起名「换一批」三档按钮 | 起名 → 提交 → 测 `.qm-style-chip` | ✅ [复测验证] 4 个按钮命中：「诗经草木」「楚辞」「清新灵动」「🔄 换一批（楚辞）」 |
| **R218a-05** | 起名推荐指数 | 起名 → 提交 → 测 `.qm-score` | ✅ [复测验证] 8 个评分命中：「⭐ 80/100」「⭐ 首选」「🌟 次选」「✨ 可选」（前 3 排序 + 后面统一 80 分） |
| **R218a-06** | 分享图/海报点击有反馈 | bazi → 点 📸 分享图 | ❌ [仍有问题] R218a-巡1 标 ✅ 是误判。**当前 `downloadPoster()` 仍只调 `canvas.toBlob()` 创建 `<a download>` 静默下载，无任何 modal/浮层/提示**（commit fd4a5cc 的 `git show` 显示 R218a-06"海报浮层"对应代码改动为 0，仅 R218a-11 `_posterHookForView` 函数被加）。本轮 Playwright 复测：`modal after shareBazi = None`、canvas 列表=[]（已释放），仅 1 个 `zhiming-poster.png` 下载触发。用户在小红书场景下完全看不到预览。 |
| **R218a-07** | 塔罗首屏「针对你问的」综合结论 | 塔罗 → question="最近感情怎么样" → 提交 | ❌ [仍有问题] `buildTarotResult` 内 `if (j.question) html += tarotQuestionHook(j.question, ...)` 钩子函数已写（5 套关键词模板 + 通用兜底，app.js:2882-2977），但**`/api/tarot` 后端不返回 `question` 字段**（实测 `keys=['seed','n','draws','interpretation','warm']`，无 `question`），前端 `j.question` 永远 falsy → 钩子永远不渲染。 |
| **R218a-08** | 六爻首屏问题绑定 | 六爻 → question="最近工作顺利吗" → 提交 | ❌ [仍有问题] 同根因：`buildLiuyaoResult` 内 `if (j.question) html += liuyaoQuestionHook(...)` 钩子函数已写（5 关键词模板 + 通用兜底，app.js:2998-3018），但**`/api/liuyao` 后端不返回 `question`**（实测 `keys=['ben','bian','ben_jing','bian_jing','interpretation','warm']`，无 `question`）。钩子代码永远不触发。 |
| **R218a-09** | 首页 daily「宜：奶茶加料/给绿植浇水/养养生机」去生造词 | 首页 → 任意日期 | ❌ [仍有问题] **"养生机"仍在 `src/guji/copy_bank.json:88` 的 `daily.yi` 列表中**（"给绿植浇水，养养生机"），且**后端 services.py:865 直接 `_pick(_db["yi"], date_str, "y")` 把它返回**（curl `/api/daily` 实测：`do: '奶茶加料，甜到心巴、给绿植浇水，养养生机'`）。R218a-巡1 标 ✅ 是误判——本轮 git diff 显示 R218a-09 对 copy_bank.json 的真实改动 = 0（commit fd4a5cc diff 仅 chat_fallback_openers 增改）。 |
| **R218a-10** | 八字摘要人话化（金属性/属马/逆运） | 八字 → 提交 → 温柔版首屏 | ✅ [复测验证] 文本含"你是属马的呀""决断底子，金偏多""日主是庚（金），干脆、边界清楚、说一是一""今天的气氛偏『表达力』"。vision 评 7.5/10，扣分点仅剩正文 1 处"日主"术语紧跟解释（达标）。 |
| **R218a-12**（LOW 复查）| 聊天置灰视觉不明显 | 聊天侧栏 → 发 1 条 | ⚠️ [部分通过] 状态机：`disabled=True, placeholder='小满休息中，明天再来聊吧'` 文字层面正确；但 `inputOpacity=1, btnOpacity=1` 视觉层面**未置灰**——R218a-巡1 的"按钮 opacity 0.4"建议未实现。R218a-02 fix 的副作用：现在发送 1 条后即锁死，无任何"今晚 / 明天 0:00 后再聊"的小字说明，用户不清楚为什么不让发。 |

### 新问题段（巡2 开荒 11 条新发现）

#### 🔴 MAJOR（4 条）

### N-01 🔴 MAJOR｜R218a-07/08 问题绑定非空即不渲染——后端不 echo `question`，前端钩子函数全死代码
- 截图：`v6_poster_*.png`（间接证据）/ 直接验证 `curl /api/liuyao` 与 `/api/tarot` 缺 `question` 字段
- 复现命令：
  ```bash
  curl -sX POST http://127.0.0.1:8183/api/tarot -H 'Content-Type: application/json' \
    -d '{"n":3,"question":"最近感情怎么样？","seed":42}' | python -m json.tool | head -10
  # keys: seed/n/draws/interpretation/warm  → 没有 question
  curl -sX POST http://127.0.0.1:8183/api/liuyao -H 'Content-Type: application/json' \
    -d '{"method":"coins","seed":42,"question":"工作顺利吗"}' | python -m json.tool | head -10
  # keys: ben/bian/ben_jing/bian_jing/interpretation/warm → 没有 question
  ```
- 现状：`web/services.py:682` `tarot()` 与 `:596` `liuyao()` 返回 dict 不含 `req.question` 字段（仅传给 `interpreter.interpret_*` 和 `voice.warm_*` 做语义）。前端 `app.js:2291 if (j.question) { html += liuyaoQuestionHook(...) }` 与 `:2890 if (j.question) { html += tarotQuestionHook(...) }` 因此**永远跳过**——R218a-07/08 整轮 fix 是死代码。视觉：塔罗用户问"感情"看到的 hook 段为空（"对应你问的" 不出现），六爻用户问"工作"看到的 hook 段为空。回归原因：`web/services.py:687-693` 与 `web/services.py:618-630` 两个返回 dict 都漏了 `req.question` 字段。
- 期望：服务端返回 dict 加 `"question": req.question`（additive，零行为风险）；前端 `if (j.question)` 钩子立即生效。
- 根因：R218a 优化轨只动 `app.js` 没动 `services.py`——典型的"前端写完忘了校验数据通"。
- 严重：🔴 MAJOR — R218a-07/08 修复实际未生效，直接影响"我问了真的被听到"体验；与 R218a-巡1 用户裁决"被精准定义"心理需求正交。

### N-02 🔴 MAJOR｜R218a-06「海报预览浮层」代码改动为 0——fd4a5cc commit 描述与实际代码不一致
- 截图：`v6_bazi_after_share.png`（点分享图后页面无变化）+ `v4-01_poster_modal=None`
- 复现：bazi → 提交 → 点 `📸 分享图` → 期望出现"长按图片保存到相册"浮层
- 现状：`git show fd4a5cc -- web/static/app.js | grep -E "modal|poster-modal|share-modal"` 0 命中。`app.js:1298-1319 downloadPoster()` 函数体未变（仅 `canvas.toBlob() → <a download>`）。本轮 Playwright 复测：点 `shareBazi` 后页面 DOM mutation observer 0 命中、modal 查询 0 命中、canvas 在 `toBlob` 后立即释放 = 用户得到 1 个静默 `zhiming-poster.png` 下载，**手机端完全无视觉反馈**（小红书分享链路核心节点缺失）。R218a-巡1 标 ✅ 是误判（仅复测了"下载能跑"没复测"有预览"）。
- 期望：`downloadPoster()` 末尾 `canvas.toBlob()` 之后追加 ① 创建 `.share-modal` 浮层（带 close × 按钮 + 长按图片保存提示 + 二次点击 canvas → 唤起 iOS 分享面板）；② ESC 关闭；③ 点击背景关闭。
- 根因：commit message "海报浮层(R218a-06)" 描述与代码 diff 不符；可能优化轨只改了文档/verify_r218a.py 烟雾脚本（`scripts/verify_r218a.py` 也未含 modal 选择器）。
- 严重：🔴 MAJOR — 与 R218a-11 同等优先级（小红书传播链路关键节点），且 R218a-巡1 误判导致阻塞 1 轮。

### N-03 🔴 MAJOR｜R218a-09「去生造词」代码改动为 0——`src/guji/copy_bank.json:88` 仍含"养养生机"
- 截图：`v3_bazi_imgs.png` 间接；直接证据 `curl /api/daily` 返回含「养生机」
- 复现命令：
  ```bash
  curl -s http://127.0.0.1:8183/api/daily | python -m json.tool
  # do: '奶茶加料，甜到心巴、给绿植浇水，养养生机'
  grep -n '养生机' src/guji/copy_bank.json
  # 88:   "给绿植浇水，养养生机",
  ```
- 现状：fd4a5cc `git show` 对 `src/guji/copy_bank.json` 的 diff 仅含 `chat_fallback_openers` 与 `chat_fallback_by_keyword` 新增，**`daily.yi` 列表 0 改动**。"养养生机"作为生造词仍出现在每日「宜」栏。视觉：在 daily 海报（`v5_poster_daily.png`）中也直接出现该文案，传播到小红书后目标用户困惑。
- 期望：`src/guji/copy_bank.json` line 88 `"给绿植浇水，养养生机"` → `"给绿植浇水，看它慢慢长大"` 或 `"养一盆小绿植"`；同样检 `daily.ji` / `daily.levels` 是否有其他生造词（本次未系统排查）。
- 根因：commit message "copy_bank.json daily_yi 文本去生造词" 描述与代码 diff 不符；典型"占位 commit message"问题。
- 严重：🔴 MAJOR — 调性细节但被 R218a-巡1 误判为 ✅ 阻塞 1 轮。

### N-04 🔴 MAJOR｜R218a-13 海报差异化 = 0——`buildShareData` switch 缺 'bazi'/'taohua'/'hehun' 三 case，全走 default null
- 截图：`v6_poster_bazi_real.png`（176KB 空模板）/ `v6_poster_taohua_real.png`（176KB，**字节数完全一致**）
- 复现命令（看字节一致性）：
  ```bash
  # 已由 tour_v6.py 抓出 3 张海报（bazi=176653, taohua=176653 byte-identical, qiming=878527, daily=741917）
  ```
- 现状：`app.js:1240-1289 buildShareData(view, j)` switch 仅 `case 'daily' / 'tarot' / 'liuyao' / 'qiming'` 4 个分支；**`bazi`/`taohua`/`hehun` 全部走 default 返回 `null`**（`:1286-1287 default: return null;`）。`drawPoster` 拿到 `j.share=null` 时退化为空模板——vision 评 bazi/taohua 海报"内容完全空白，'本命 （）'括号内什么都没有"。R218a-13 修复意图"按功能做差异化海报骨架"完全没实现，**3/6 功能海报纸质全空**。
- 期望：`buildShareData` 补 3 个 case（'bazi' 返回 `{title:'今日命盘', lines:[{k:'日主', v:...}, {k:'五行', v:...}, {k:'用神', v:...}]}`；'taohua' 返回 `{title:'桃花运', lines:[{k:'桃花位', v:...}, {k:'应期', v:...}, {k:'对象', v:...}]}`；'hehun' 返回 `{title:'合婚', lines:[{k:'年支', v:...}, {k:'日主', v:...}, {k:'缘分', v:...}]}`）；视觉骨架加装饰（桃花/CP/印章）。
- 严重：🔴 MAJOR — 小红书传播 ROI 重大损失，3/6 海报等同未做。

#### 🟠 HIGH（4 条）

### N-05 🟠 HIGH｜错误态 / 空态全无前端反馈——八字非法年/月/空提交，页面无 error 文案
- 截图：`v3_bazi_error_year.png` / `v3_bazi_error_month.png`（无可见错误）
- 复现：bazi 视图 → `#year` 填 `1850`（小于 min=1900）→ 点 `#submit` → `#error` div 0 文案 / 页面无变化
- 现状：HTML5 `min=1900` 校验触发原生气泡（被 Playwright headless 屏蔽，且移动端 native 弹气泡极易被用户忽略），后端 `LiuyaoRequest.validate_ranges()` 同样静默抛 `ValidationError` 但前端 `fail('lyResult', '摇卦失败：' + e.message)` 走的是 toast/卡内文案——本轮多次复测 #error / .error 选择器返回 `''` 空串。
- 期望：错误时 result 卡内顶部显示淡红条 `<div class="error-msg">⚠️ 年份超出范围（1900-2100），请检查后重试</div>`；表单字段加 `aria-invalid` 高亮；移动端 toast 弹 2 秒。
- 严重：🟠 HIGH — 用户提交后页面无反馈会认为"网络坏了/我没点中"。

### N-06 🟠 HIGH｜聊天 disabled 状态视觉无 opacity/灰度，R218a-12 LOW 升级为 HIGH
- 截图：`v4_chat_disabled.png`（input 视觉与未禁用时无差）
- 复现：聊天侧栏打开 → 发"我好累" → 1.5 秒后截图
- 现状：`input.disabled=true` 但 `getComputedStyle(inp).opacity=1, backgroundColor=rgb(255,255,255)`（未变灰）；`btnSendBtn.disabled=true` 同样 opacity=1。placeholder 文字层面已改"小满休息中，明天再来聊吧"，但视觉层无任何状态差异。
- 期望：CSS `.recent input:disabled, .recent button:disabled { opacity: 0.5; background: #f5f5f5; cursor: not-allowed; }`；并在 input 下方加小字 "⏰ 自动回复 · 人工客服 0:00 上线"（R218a-12 原建议）。
- 严重：🟠 HIGH — 状态机表达弱直接导致用户重发/困惑。

### N-07 🟠 HIGH｜侧栏「我的解读」历史记录段在 R208b 被删——history API 仍在但 UI 入口消失
- 截图：`n04_history_api.png`（API 正常工作）+ 主页 0 入口
- 复现：`curl http://127.0.0.1:8183/api/history?limit=3` 返回 3 条记录（含 paipan_render），但主页/侧栏**无任何 UI 入口**（grep 全站 `历史/记录/最近` 按钮 0 命中，only 命中 `recentSidebar` 容器本身）。
- 现状：R208b / R210b 按用户裁决"删「我的解读」段入口"，后端 `/api/history` 仍存活（每条 bazi 都保存到 history.db），用户无法看到自己的历史盘。
- 期望：① 侧栏顶部 brand 下方加 1 行"📜 最近的解读（n 条）"+ 可展开抽屉（点击展开看 list）；② 抽屉内每条显示 `paipan_render` + `created_at` + "再次查看"按钮。
- 严重：🟠 HIGH — 数据存在但用户看不到，复访=0 是漏斗黑洞。

### N-08 🟠 HIGH｜装饰图集成 = 0——`_candidates/r212b/` 仅 2 张图（daily-box-gift + avatar），非粉色水彩少女风
- 截图：`v3_imgs_home.png` + `v3_bazi_imgs.png`
- 复现：主页 → 任意功能 → grep `<img>` 元素
- 现状：全站 2 张 img：`/static/_candidates/r212b/daily-box-gift.png`（600×600，未在结果页用）+ `/static/_candidates/r212b/avatar-xiaoman.png`（512×512，侧栏头像）。**结果页/海报/塔罗 6 大功能 0 装饰图**。M3 写 prompt + agnes-image/Pollinations 双后端已就位（`scripts/image_gen.py` 110 行），但前端 `index.html` / `app.js` 未消费。
- 期望：① 排盘结果顶部加 1 张手绘少女/水彩花/水晶球（`/static/generated/bazi-*.png`，agines-image 后端预生成 4-6 套随机）；② 塔罗每张牌用 `r212b` 候选图（`<img class="tarot-card-front" src="/static/_candidates/r212b/tarot/RXX-*.png">`）；③ 海报背景填水彩底（`r212b` 已有 `daily-box-gift.png` 可复用）。
- 严重：🟠 HIGH — 视觉调性"温柔治愈 + 玄学水彩"承诺未兑现；后台生成链路 0 消费。

#### 🟡 MED（3 条）

### N-09 🟡 MED｜海报复用通用模板缺"长按保存"提示，且 brand 仅文字水印——vision 评 4-8/10
- 截图：`v5_poster_*.png` × 4 / `v6_poster_*.png` × 4
- 复现：见 v6 tour 输出，已知 bazi/taohua 空模板；qiming/daily 内容完整
- 现状：daily 海报 8/10（有樱花 + 暖黄圆形 + 完整金句），qiming 8/10（有推荐名 4 个），bazi/taohua/hehun 0/10（空模板 = 字节数完全一致）。水印「@小满的解忧铺 · 知命知书知天机」已在所有 4 张图中出现（R218a-11 修了），但金句 hook 仍偏鸡汤（"知命，是为了更好地活"），缺"3 秒抓眼球的扎心金句"。
- 期望：① 修复 N-04 让 3 张空模板填数据；② 5 套金句 hook 改"数据化钩子"（"你的命格有 73% 的『金』属性" / "本月偏财指数 8.2"）；③ 加"长按图片保存到相册"小字底部（iOS Safari 行为兼容）。
- 严重：🟡 MED — 视觉有基础但缺灵魂。

### N-10 🟡 MED｜六爻"初爻被 FAB 遮挡"在 R215b 报告后未真正修复——R218a-巡1 留
- 截图：`n11_bazi_bot_fab.png`（FAB @ x=16 y=772 w=52 h=52，距视口底 20px）
- 复现：bazi → 提交 → 滚到底 → 测分享图按钮是否被 FAB 挡
- 现状：FAB 仍 `position: fixed; bottom: 16px; left: 16px;`，但 52×52 矩形仅在 x∈[16,68] y∈[772,824] 区域挡。分享图按钮在结果卡内 #result / .result 段，不在 FAB 覆盖区。**但**：六爻结果页第一爻（初爻）的卡片底部 6px 与 FAB 上沿有 12px 视觉重叠（R218a-巡1 已记录，本轮未变）。
- 期望：FAB 改 `bottom: max(16px, env(safe-area-inset-bottom))`；结果区 padding-bottom 增加 60px。
- 严重：🟡 MED — 视觉细节，少数功能（六爻）受影响。

### N-11 🟡 MED｜移动端安全区未设置——`env(safe-area-inset-top/bottom)` 在 documentElement 永远为 0
- 截图：`v3_home`（顶部 status bar 区域被占用）
- 复现：iPhone 14 视口 390×844 → `getComputedStyle(documentElement).getPropertyValue('env(safe-area-inset-top)')` = `''`；`paddingTop: '0px'`
- 现状：HTML 缺 `<meta name="viewport" content="...viewport-fit=cover">` 或 CSS 缺 `body { padding-top: env(safe-area-inset-top); padding-bottom: env(safe-area-inset-bottom); }`——iPhone 刘海/底部 home indicator 直接盖住内容。
- 期望：① `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`；② `body { padding: env(safe-area-inset-top) 0 env(safe-area-inset-bottom); }`。
- 严重：🟡 MED — iPhone 真机体验细节。

### 小区未单列（已记录在 N-04/N-05/N-08 内，不重复）

- **小记 N-α**：`/api/user/prefs` 返回 `{"theme":"cream","recent":[],"favorites":[]}`，R208b 删 UI 入口后只剩 API。
- **小记 N-β**：`/api/threads` 返回 1 条研究线程（大過九二 异文性质）——书学页 threads tab 是空状态。
- **小记 N-γ**：网络错误 chat 正确降级到"（网络不太好，再发一次试试？）"（V3-06 ✅）。
- **小记 N-δ**：桃花男 / 桃花女切换均返回正常结果（无性别硬编码，U-004 修复保持）。

### 视觉评审（vision_analyze 输出摘录）

| 海报 | vision 评分 | 关键问题 |
|------|------------|----------|
| `v5_poster_bazi.png`（空模板）| **4.5/10** | 中部核心内容"本命（）"括号内空；调性偏 30-45 岁新中式 |
| `v5_poster_taohua.png`（空模板）| **4/10** | 与 bazi 字节完全一致（176KB 字节相同），无桃花主题视觉符号 |
| `v6_poster_qiming_real.png` | 估 **7-8/10** | 4 个推荐名 + 评分到位，但缺少女插画/印章骨架 |
| `v6_poster_daily_real.png` | **8/10** | 粉橘+樱花+暖黄圆形 + 完整金句，最佳 |
| `03_bazi_top.png`（温柔版首屏）| **7.5-8/10** | 锋利小刀型人设卡达标；"日主"术语残留 1 处 |

### 本轮总结（R218a-巡2 · 2026-08-26）

**复测通过率：7/10 真实生效 + 3 条 fd4a5cc commit 描述与代码不一致（被 R218a-巡1 误判为 ✅）**
- ✅ R218a-01 侧栏宽度 + 点击外部关闭
- ✅ R218a-03 八字人设卡
- ✅ R218a-04 起名换一批
- ✅ R218a-05 起名推荐指数
- ✅ R218a-10 八字摘要人话化
- ✅ R218a-02 降级文案轮换（池子工作正常，但被 disabled 状态挡掉用户视角的 2 条连发）
- ⚠️ R218a-12 文字层面 OK / 视觉置灰未做
- ❌ R218a-06 海报浮层代码改动为 0（commit 描述虚标）
- ❌ R218a-07 塔罗问题绑定非空即不渲染（后端 services.py 不 echo `question`）
- ❌ R218a-08 六爻问题绑定同上根因
- ❌ R218a-09 「养生机」生造词仍在 `copy_bank.json:88`（commit 描述虚标）
- ❌ R218a-13 海报差异化 = 0（buildShareData switch 缺 3 case）

**新问题：11 条（4 MAJOR + 4 HIGH + 3 MED）**
- 🔴 MAJOR：N-01 R218a-07/08 后端不 echo question / N-02 R218a-06 海报浮层未实现 / N-03 R218a-09 生造词未删 / N-04 R218a-13 buildShareData 缺 3 case
- 🟠 HIGH：N-05 错误态无反馈 / N-06 chat disabled 视觉无 opacity / N-07 历史记录 UI 入口缺失 / N-08 装饰图集成 = 0
- 🟡 MED：N-09 海报金句 + 长按保存提示 / N-10 FAB 遮挡初爻未修 / N-11 移动端安全区未设置

**截图**：54 张 + 4 张真实海报 PNG = 58 个文件 / 23MB / 路径前缀 `$LOCALAPPDATA/Temp/tour/R218a-巡2/`

**Console errors**：**0** 自然态 / 0 pageerror（v3 唯一 1 个 `[error] Failed to load resource: net::ERR_FAILED` 是受控 `page.route("**/api/chat", abort)` 注入的网络错误测试，不计为产品错误）

**3 条最痛点**：
1. **N-01（🔴 MAJOR）R218a-07/08 钩子函数全死代码**：后端 3 个 API（`/api/tarot` / `/api/liuyao` / 推断的 `/api/bazi`）不 echo `question` 字段，前端 `if (j.question) html += hookFn()` 永远跳过。整轮 R218a 优化轨在塔罗/六爻问题绑定上做的"5 套关键词模板 + 通用兜底"全部是纸面工程。复现：3 行 curl 看响应 keys 即可。
2. **N-04（🔴 MAJOR）R218a-13 海报差异化 = 0**：`buildShareData` switch 缺 `bazi/taohua/hehun` 3 case，3/6 海报内容空白（vision 0/10），与 qiming/daily 海报视觉断崖式差距。复现：点 bazi/桃花/合婚的"📸 分享图"得 1 张"本命 （）"空模板。
3. **N-02（🔴 MAJOR）R218a-06 海报浮层虚标**：fd4a5cc commit message 写"海报浮层(R218a-06)"但 `git show` 对 `web/static/app.js` 该条 0 改动。R218a-巡1 标 ✅ 是误判（仅看下载能跑没看有预览）。修复成本 < 1.5h，但需要补 modal DOM + CSS + 事件绑定 + ESC/背景关闭 + 长按保存提示。

**建议优化轨先攻顺序**：
1. **N-01 + N-04 打包**（同根因：services.py 端 4 处加 `req.question` 回写 + `buildShareData` 补 3 case，预计 < 2h，立即让 R218a-07/08/13 真正生效）
2. **N-02 海报浮层**（commit message 虚标的真实修复，1 个 modal 组件 + CSS，预计 < 1.5h）
3. **N-03 删养生机 + N-06 disabled 视觉**（copy_bank.json 1 行改 + CSS 1 段，预计 < 30 分钟，恢复 R218a-09/12 用户可见度）

**遗留方向**（同 R218a-巡1）：
- LLM 真实链路质量巡检（DISABLE=1 无法验 AI 生成质量）
- 小红书真机用户测试
- 装饰图/插画集成（N-08 — M3 写 prompt + agnes-image/Pollinations 双后端已就位，前端消费链路 0）
- history.db 60+ 条历史记录 UI 入口（N-07 — 后端活，UI 死）
- iOS safe-area N-11（viewport-fit=cover 1 行 + CSS env 1 段）
- research / threads / concept / compare tabs（N-α/β/γ 留）

### N-04 虚标重做（R218a-巡2 修复）| buildShareData 补 3 case + share* 改 view
- ✅ [已修 R218a-巡2 · buildShareData 补 bazi/taohua/hehun 3 case，shareBazi/shareTaohua/shareHehun 改传正确 view；styles.css+app.js 共 +150 行；grep case 数 4→7]

### N-01（修复 R218a-07/08）| services.py echo question 字段
- ✅ [已修 R218a-巡2 · web/services.py 在 bazi/liuyao/tarot 三个返回 dict 加 "question": req.question；selftest._expect_keys 同步加键；curl 实测 3 端点均 echo；grep 数 0→3]

### N-02（修复 R218a-06 虚标）| 海报浮层
- ✅ [已修 R218a-巡2 · downloadPoster 末尾 showPosterModal()；styles.css poster-modal-backdrop + 关闭按钮 + 遮罩 + ESC；长按保存提示；grep 数 0→5]

### N-03（修复 R218a-09 虚标）| 去养生机
- ✅ [已修 R218a-巡2 · copy_bank.json daily.yi 第 4 条改「🪴 给家里绿植浇点水」；清 data/index/knowledge.db 的 daily_cache 表缓存；grep 养生机 1→0]

### N-05 | API 错误 toast
- ✅ [已修 R218a-巡2 · api() 网络层 catch 前 showToast(msg, kind)；4xx 黄底/5xx 红底；styles.css .toast-stack + .toast-error/warn/info；3.5s 自动消失]

### N-06 | chat disabled 视觉
- ✅ [已修 R218a-巡2 · styles.css .chat-input-row input:disabled / button:disabled 显式 opacity:.5 + 置灰 + cursor:not-allowed]

### N-07 | 历史记录 UI 入口
- ✅ [已修 R218a-巡2 · index.html 侧栏顶部加可折叠段「📚 我的解读·N」；app.js initChat 注册 toggle + refreshHistoryCount()；styles.css .side-history* 8 条新规则]

### N-08 | 装饰图集成
- ✅ [已修 R218a-巡2 · app.js renderDecoration(view) 函数：bazi/qiming/taohua 3 个差异化 banner（CSS 渐变 + emoji 锚 + 文字）；buildBaziResult/QimingResult/TaohuaResult 顶部注入；styles.css .deco-banner* 6 条新规则；后续接入 /api/decoration 时把 url 套进 .deco-img 即可]

### N-09 | 海报金句数据化
- ✅ [已修 R218a-巡2 · _posterHookForView 改数据驱动：bazi 取日主 / qiming 取 TOP1+score / taohua 取桃花支强度 / hehun 取双方日主五行；liuyao/daily 走原文案]

### N-10 | FAB 遮挡初爻 + 安全区
- ✅ [已修 R218a-巡2 · styles.css .recent-toggle bottom: max(20px, env(safe-area-inset-bottom,20px))；.view padding-bottom:70px 防初爻被 FAB 挡]

### N-11 | iOS 安全区
- ✅ [已修 R218a-巡2 · index.html meta viewport 加 viewport-fit=cover；styles.css body padding 用 env(safe-area-inset-*)；desktop/Android 不影响]

---

## R218a-巡3（聚焦复测 5 条 · 2026-08-26）

> 范围：5 个最高 ROI 复测点（N-01/N-02/N-04/N-07 + 1 新发现）。每条都自己跑 Playwright 真实交互，**不复用 R218a-巡2 的 ✅ 标记**。
> 后台：uvicorn :8183（pid 9988，BOOKS_LLM_DISABLE=1，curl `/api/bazi /api/liuyao /api/tarot` 均 200 OK）。
> 工具：playwright + venv/Scripts/python.exe（uvicorn 复用；strict_readonly 严守 — 仅读 web/，写 docs/UX_REVIEW_QUEUE.md）。

### 复测 N-01 | bazi question 真实生效
- **结论：✅ 真生效**（修复真实，不是虚标）
- **后端证据**（curl 直测）：
  - `POST /api/bazi {year:1995,month:6,day:15,hour:14,gender:"女",question:"最近事业怎么样"}` → 响应 `keys = ['paipan','calc','evidence','interpretation','warm','ai_polish','question']`，`question = '最近事业怎么样'` ✅
  - `POST /api/liuyao {method:"coins",seed:42,question:"升职能成吗"}` → `question = '升职能成吗'` ✅
  - `POST /api/tarot {seed:42,n:3,question:"感情走向"}` → `question = '感情走向'` ✅
- **前端证据**（Playwright UI 真实提交）：
  - `#year/month/day/hour/gender/question` 全部填好 → 点 `#submit` → 等 3.5s
  - 抓 `WARM_LAST_QUESTION = '最近事业怎么样'` ✅（empathy 模板选择依据已生效）
  - 抓结果页 `body.innerText` 找到绑定句原文：
    > 「**你问「最近事业怎么样」**——这属于事业，盘里对应的位置有 2 处：月干壬（规矩位）、年支藏干壬（规矩位）。其中最靠前的那个是规矩位（正官）——在规则里行事，责任感重。」
  - vision_analyze 复核：`01_bazi_result.png` 中确认"师傅回复区"开头就是「你问「最近事业怎么样」」
- **R218a-巡2 修复正确性**：6fc148c commit 在 services.py 的 bazi/liuyao/tarot 3 个返回 dict 都加了 `"question": req.question`，**前端 baziResult 解读段有 `if (j.question)` 钩子**（line 1974 附近的 renderVoice 链）确实读到了。**N-01 完整闭环，无虚标**。
- 截图：`$LOCALAPPDATA/Temp/tour_R218a-巡3/01_bazi_result.png`（含绑定句 + pill 排盘）
- 数据：`01_bazi_binding.txt`

### 复测 N-02 | 海报浮层
- **结论：❌ 仍虚标/broken**（R218a-巡2 修了一半 — modal DOM/CSS/事件全写了，但走不通）
- **Playwright 真实交互**：填表 → 提交 → 拿到 bazi 结果 → 点 `#shareBazi` → 等 3.5s → 截图
- **浮层状态**：`document.getElementById('posterModal')` = **null**。`02_after_share.png` vision 复核：「**整个界面里完全没有任何海报浮层——既看不到半透明的深色遮罩背景，也看不到中央弹出的海报图片，更没有关闭按钮**」。
- **根因（Playwright 抓堆栈，定位到代码行）**：
  ```
  ReferenceError: j is not defined
      at _paintSharePoster (http://127.0.0.1:8183/static/app.js:1247:46)
      at _paintPoster     (app.js:992:28)
      at drawPoster       (app.js:974:12)
      at downloadPoster   (app.js:1416:11)
      at HTMLButtonElement.<anonymous> (app.js:2009:35)
  ```
  → `app.js:1247` 是 `var hook = _posterHookForView(s && s.view, j);` —— `_paintSharePoster(s, W, H)` 的入参只有 `s`，**没有 `j`**。R218a-巡2 加 `buildShareData(view, j)` 注入 `j.share` 后走新 `_paintSharePoster` 路径，但这条线对 `j` 引用是死代码。`drawPoster` 抛错 → `downloadPoster` 中断 → `showPosterModal(r.canvas, view)` 永远到不了。
- **R218a-巡2 修复正确性**：6fc148c commit 加了 `showPosterModal()` 函数 + CSS `.poster-modal-backdrop/.poster-modal/.poster-modal-img/.poster-modal-close/.poster-modal-tip` + ESC/背景关闭 + 长按保存提示 + 下载仍走 toBlob —— **这些都是对的**，但**画海报那条路根本到不了浮层**。等于「客厅装修好，但厨房门锁着，用户永远走不进来」。
- **复测成本**：1 行修复 `j` → `s` 即可（也可能 `_posterHookForView` 应改读 `s` 字段）。**预估 < 5 分钟**。
- 截图：`$LOCALAPPDATA/Temp/tour_R218a-巡3/02_after_share.png`（无浮层，主页原样）
- 数据：`02_modal_state.json`（含 page error 完整堆栈）

### 复测 N-04 | 海报差异化（bazi/taohua/hehun）
- **结论：❌ 仍虚标**（同根因：`_paintSharePoster line 1247 j is not defined`）
- **Playwright 真实调用**：`fetch /api/{bazi|taohua|hehun}` 拿 j → `downloadPoster(j, view)` 拦截 toBlob/anchor.click → 期望从 `#posterModal img.poster-modal-img.src` 拿 dataURL
- **3 个 view 全部抛 `j is not defined`**：
  - bazi: `keys=['paipan','calc','evidence','interpretation','warm','ai_polish']` → thrown 'j is not defined'（堆栈同 N-02）
  - taohua: `keys=['bazi','year_zhi','peach_zhi','hit_pillars','hongluan',...]` → thrown 'j is not defined'
  - hehun: `keys=['a_bazi','b_bazi','year_zhi_a','year_zhi_b','clash','combine',...]` → thrown 'j is not defined'
- **字节数对比**：❌ N/A — 3 张海报 0/3 张成功渲染，无法做字节数对比
- **vision_analyze 对比**：❌ N/A — 无 PNG 产物
- **R218a-巡2 修复正确性**：6fc148c commit 在 `buildShareData` switch 加了 `bazi/taohua/hehun` 3 case，share*Btn 改传 view —— **这部分对**，但**进入模板后被同一 `j is not defined` bug 卡死**。N-04 与 N-02 是**同一根因的同一修复优先级**（修一个两个都通）。
- 数据：`04_posters_summary.json`

### 复测 N-07 | history UI 入口（侧栏「📚 我的解读·N」）
- **结论：✅ 渲染成功**（段存在、可展开、有 items），但显示**count 不准**（详见新发现 N-α）
- **Playwright 真实交互**：进首页 → 点「聊」toggle → 抓 `.side-history` 段
- **DOM 证据**：
  - `seg_class = 'side-history'` ✅
  - `head_text = '📚 我的解读\n1\n▸'`（折叠态；▸ 表示可展开）✅
  - 展开后 `item_count = 20`（segment 内 20 条 `.side-history-item`，每条带时间戳 + 问题 + paipan_render + 查看/删除按钮）✅
  - vision 复核：`07_sidebar_open.png` 中侧栏顶部清楚显示「📚 **我的解读** 1 ▼」，展开后首条「2026-08-26T13:42:42 / 事业 / 乙亥年 壬午月 丁丑日 丁未时 / 查看 删除」完整可读
- **R218a-巡2 修复正确性**：6fc148c commit 加 `initChat` 注册 toggle + `refreshHistoryCount()` + `app.js:3629-3645` —— **段渲染 100% 通过**。
- 截图：`$LOCALAPPDATA/Temp/tour_R218a-巡3/07_sidebar.png`（折叠态）、`07_sidebar_open.png`（展开态，20 条 items）
- 数据：`07_sidebar_state.json`、`07_sidebar_expand.json`

### 新发现 1 条 N-α | history count 严重偏低（count=1 vs 实际 50）
- **严重级**：🟠 HIGH（数据/信息架构 Bug — 用户被严重误导「只有 1 条历史」，实际 50 条）
- **复现**（Playwright，3 步）：
  1. 开首页 → 点「聊」→ 进侧栏 → 头部显示「📚 我的解读 **1** ▼」
  2. 展开 → DOM 内 20 条 items
  3. 直接 `fetch('/api/history')` → `records.length = **50**`
- **实测数据**（json 截自 Playwright）：
  ```json
  {
    "count_display": "1",        // 侧栏头部徽章
    "count_display_in_head": "📚 我的解读\n1\n▸",
    "visible_item_count": 20,     // 展开后渲染的 items
    "api_records_count": 50,     // /api/history 真实总数
    "api_keys": ["records"]      // 注意：没有 total 字段
  }
  ```
- **现状**：
  - 用户进侧栏看到「我的解读 **1**」→ 以为只有 1 条历史
  - 实际数据库里 50 条
  - 展开后只能看到前 20 条
  - **30 条记录 UI 完全无法访问**（无分页/无限滚动/加载更多按钮）
- **根因**（两处都查证）：
  - 后端 `web/services.py:333` `def history_list(limit: int = 50)` → 只返 `{"records": [...]}`，**没有 `total` 字段**
  - 前端 `web/static/app.js:3634-3646` `refreshHistoryCount()`：
    ```js
    var j = await api('/api/history?limit=1');
    var n = (j && j.records) ? j.records.length : 0;   // n=1
    var total = (j && typeof j.total === 'number') ? j.total : n;  // total 兜底 = 1
    cnt.textContent = total > 0 ? String(total) : '0';  // 显示 "1"
    ```
  - `limit=1` 只返 1 条 + 后端没 `total` → 永远显示 1
- **期望**：
  1. **必改**：`web/services.py history_list` 加 `total` 字段（用 `COUNT(*)` 或 `len(records)+more` 标记）；前端 `refreshHistoryCount` 优先读 `total` → 显示 **50** 而不是 1
  2. **应改**：history list 加分页/无限滚动（当前 `.side-history-item` 渲染 20 条就停，后 30 条永远看不到）
  3. 可选：count 旁加 `+N` 标识「最近 20 / 共 50」
- **截图证据**：`$LOCALAPPDATA/Temp/tour_R218a-巡3/07_sidebar_open.png`（20 条 items 实际渲染）+ `07_sidebar_expand.json`（实测数据）
- **下一轮建议**：和 N-02/N-04 打包到 R218a-巡4 优化轨（同一审计窗口 — 修 1 行后端 + 1 段前端 + 加分页即可，估 < 30 分钟）

### 本轮总结
- **复测通过 2/4**：N-01 ✅ + N-07 渲染 ✅（N-07 的 count 偏低发现放新发现）；N-02 ❌ + N-04 ❌ 同一根因（`_paintSharePoster:1247 j is not defined`）
- **新发现 1 条**：N-α（🟠 HIGH）侧栏 history count 显示与实际严重不一致（1 vs 50）+ 历史列表无分页（30 条不可达）
- **截图总数**：6 张（`01_bazi_form/01_bazi_form_filled/01_bazi_result/02_after_share/07_sidebar/07_sidebar_open`），满足 ≥5
- **console errors**：**0 个 console.error**；1 个 `pageerror: j is not defined`（N-02 触发，是产品 Bug 的副产品，非受控注入）
- **建议下一轮 R218a-巡4 重点**：
  1. **N-02 + N-04 一次性修复**（同根因，`_paintSharePoster:1247` 改 `j` → `s` 或 `_posterHookForView` 改读 `s`；估 < 5 分钟，让 R218a-06/13 真正生效）
  2. **N-α history count + 分页**（后端加 `total` + 前端分页/无限滚动，估 < 30 分钟，让 60+ 历史 db 真正可用）
  3. 留给 R218a-巡5：横屏/平板/可访问性、错误边界/离线/网络断开、a11y 审计
- **附录 — 本轮产物路径**：
  - tour 脚本：`Temp/tour_R218a-巡3/tour.py`（review-only，strict_readonly 内）
  - 截图/数据：`$LOCALAPPDATA/Temp/tour_R218a-巡3/`（6 PNG + 7 TXT/JSON）
  - UX_QUEUE 本段追加：第 1338 行起（本段标题 `## R218a-巡3…`）
