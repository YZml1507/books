# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Vendor / zipped deps (not source we own)
vendor/
Agent-Reach-main/
Agent-Reach-main.zip
markitdown-main/
markitdown-main.zip
browser-use-main/
browser-use-main.zip

# Build / cache artifacts
*.db-journal
*.log

# logs/ — runtime logs, screenshots, scratch probes (not deliverables)
logs/
# P2 scratch: downloaded git repos (82MB), not source we own
logs/p2_tmp/

# Local secrets — never commit the filled-in LLM config
llm_config.json

# OS junk
Thumbs.db
desktop.ini

# Temp probes (scratch files, not deliverables)
temp_*.py

# IDE
.vscode/
.idea/

# PyInstaller build output (217MB exe, not source)
dist/
build/
*.spec.bak
