# 最终验收清单（2026-08-28 水墨命理改版）

对照 Goal Brief acceptanceCriteria 逐项打勾，证据均可复查。

| # | 验收项 | 结论 | 证据 |
|---|---|---|---|
| 1 | 完成项目现状盘点 | ✅ | docs 同款清单 `.cluster/audit.md`：11 个视图 + 后台分层 + 8 项缺陷逐条 |
| 2 | 优化方案成文、改动点明确 | ✅ | `.cluster/plan.md`：A 视觉 6 项 / B 图标 6 组 / C 后台 7 项，含优先级、风险、回退；重大改动（换肤、历史重建）在 D 节标注 |
| 3 | 新页面风格全面落地 | ✅ | styles.css :root 令牌全量换水墨色板（令牌名不变）；桌面+移动截图 `.cluster/shots/home_desktop.png`、`home_full.png`、`home_mobile_v2.png` AI vision 走查：无破图、无错位、无新旧混杂 |
| 4 | API 生成的图标图片已接入 | ✅ | 14 张 agnes-image-2.1-flash 生成（13 张接入页面：hero/logo/8 功能图标/背景/头像/favicon）；截图确认全部渲染；归档 `web/static/ink/`（.jpg 网页版 + *-full.png 源文件 + manifest.json 引用清单 + docs/assets-manifest.md） |
| 5 | 后台功能增强可用 | ✅ | 排盘历史台账 5 端点 HTTP 实测（写入/分页/单条/删除/CSV 导出）；Playwright 实点：复看详情打开、删除 -1、导出触发下载；AI 聊天实测回复正常 |
| 6 | 排盘结果回归一致 | ✅ | `.cluster/regression/regression_report.md`：8 用例（含闰月、23 点/0 点子时边界、农历、男女）改版前后逐一对比，8 一致 / 0 差异 |
| 7 | 排盘记录可追踪 | ✅ | data/paipan_history.db（新库）；刷新/重启后记录仍在（walkthrough2 history_persists=true，50 条全在）；测试数据已清零交还用户 |
| 8 | 异常情况有兜底 | ✅ | 非法时辰=99 → 前端校验拦截不发请求；不存在记录 → 404 中文「排盘记录不存在：#9999」；图片 onerror 兜底；JS console 零报错（walkthrough js_errors=NONE）；API key 失效 → AI 入口自动降级（既有机制+实测降级路径存在） |
| 9 | 原项目已备份且可回滚 | ✅ | 完整备份 `books_backup_20260828`（排除 .venv/.git 等可再生目录）；docs/rollback.md 三种回滚方式；git stash 实际演练：还原→验证→恢复新版全通过 |
| 10 | 交接文档齐全 | ✅ | docs/handover.md：启动（双击/开发模式）、key 更换、目录结构、8 条 FAQ 排障、安全边界 |
| 11 | 最终验收走查通过 | ✅ | Playwright 全流程：首页加载→填生辰→排盘出结果→打开历史→复看→删除→导出→异常输入→刷新持久化，全链路无阻断、无 JS 报错 |

## 遗留说明（非阻塞）
- 自测断言按新事实更新：首页卡 8→9（新增排盘历史卡）；`web selftest.py` 161 checks PASS（BOOKS_LLM_DISABLE=1 闸门环境，与原设计一致；日常环境跑需临时设此变量，已写入 handover.md FAQ）。
- AI key 此前在文件中已完整（51 位），本次验证连通性而非改写；聊天/润色实测可用。
- 移动端历史横向溢出已修（body overflow-x:hidden），复查 scrollWidth-clientWidth=0。
- subagent 通道当日故障（LLM request failed ×4），全部工作由主线完成，过程记录在 .cluster/plan.md cluster_bypass_reason。
